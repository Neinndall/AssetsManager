>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

AssetsManager - League of Legends | v4.1.0.0

MAJOR UPDATE
This hotfix separates the BIN diffing and WAD comparison work from the previous release line. It focuses on semantic BIN output, faster WAD comparisons, and the optional-value crash fix.

New Features
  - VFX Studio now applies authored stencil masks so clipped particles and meshes render without visible mask geometry.
  - VFX Studio now reconstructs animation ParticleEventData sequences, resolves their ResourceMap effects, plays resolved events with authored start/kill timing, and separates optional preview loops from emitter lifetimes.
  - VFX AttachedMesh emitters now resolve only their authored owner submeshes with the champion skin scale; unresolved owner context renders no proxy model.
  - VFX Studio now shares one loading/resource cache per window and releases OpenGL, decoded texture, mesh, animation, and index resources when closed.
  - Viewer / Model materials now animate BIN-authored vertex waves and Fresnel noise when the material declares those features.
  - Viewer / Model materials now apply BIN-authored Fresnel, masked emission, and simple dissolve transitions alongside animated scroll and flow textures.
  - Viewer / Project Browser can load multiple selected animation files from its context menu.
  - Viewer / Map Environment now applies map-authored sun, ambient lighting, and baked lightmaps to static geometry.
  - Chroma Library now groups variants by their source skin with family selection and a persistent selected count.
  - Viewer models can now be selected directly in the viewport and moved along XYZ axes with a transform gizmo, including CTRL toggles and SHIFT ranges.
  - Viewer can now auto-arrange selected models from left to right around the scene center using their model-list order.
  - Tree explorers and Grid View now support SHIFT and CTRL+SHIFT range selection.
  - VFX Studio / Extended the filtered BIN audit with emitter importance, spawn volumes, motion values, child links, and decoded mesh bounds for reproducible scale and placement diagnostics.
  - VFX Studio / Added multi-layer Emitters / Layers timeline deck matching Riot editor layout, placing Master VFX System bar at Row 0 with emission delay markers for secondary emitters.
  - VFX Studio / Added per-emitter visibility toggles so individual particle and mesh layers can be isolated in the viewport during playback.
  - VFX Studio / Added automatic dependency resolution from BIN headers (PROP v2+ Dependencies) to recursively pre-load companion sub-systems, static meshes, textures, and sounds.
  - VFX Studio / Added CPU skinning for authored `.skn + .skl + .anm` particle meshes and indexed skeleton/animation resources from extracted WAD trees.
  - VFX Studio / Enabled full camera interaction parity (Orbit, Pan, Shift turbo zoom, Ctrl precision zoom, cursor indicators) matching 3D Model Viewport.
  - Viewer / Added selectable VFX playback with play, pause, stop, and speed controls.
  - Hash Guessing Lab / Added interactive sub-algorithm checkboxes and Select/Deselect All tools per attack suite, allowing granular single-attack execution alongside instant single-click actions.
  - Hash Guessing Lab now shows every GAME and LCU match as soon as it is discovered.
  - Hash Guessing Lab / Added a dedicated 'Search Unknown Hashes' action to scan local WAD files and build the unknown hash dictionary independently.
  - Hash Guessing Lab / Added a dedicated Banner Guess action for esports sponsored banner hashes.
  - Hash Guessing Lab / GAME WAD grep now follows animation BIN `mAnimationFilePath` WadChunkLinks and derives contextual `.anm` GAME candidates from the known path corpus, including skin-number variants.
  - Hash Guessing Lab / GAME shader guessing now discovers executable-referenced HLSL families and immediate `_0` variants for generated platform targets.
  - Hash Guessing Lab / Added the GAME Custom Guess button with a focused BIN-basename wordlist attack using deterministic 30,000-entry build lists.
  - Hash Guessing Lab / GAME Custom now also targets deterministic basename substitutions from `data/*.bin` paths.
  - Hash Guessing Lab / GAME Custom now also targets deterministic basename substitutions from character `.dds` paths.
  - Hash Guessing Lab / GAME Custom now includes character `.tex` substitutions and deterministic basename word addition.
  - Viewer / Added configurable ground logo opacity in General settings.
  - Viewer / Added a configurable ground logo size in General settings.
  - Viewer / Added a transparent ground logo overlay that preserves the original image resolution.
  - Added independent BIN and RST hash discovery with typed local inventories, same-file BIN evidence, contextual RST strategies, and automatic persistence.
  - BIN differences now show only semantic object and property changes with resolved paths.
  - Added centralized and persistent tracking of unknown hashes in hash_lab/unknowns.txt, merging historical unknowns with live WAD scans.
  - Integrated advanced LCU TFT patterns (24 different path combinations) inside LCU Advanced guessing suite.
  - Enhanced numeric variant guessing to support padded formats (2-digit D2 and 3-digit D3 layouts).
  - Added Prefix Attack (check_basename_prefixes) inside GAME Extended guessing suite to target tft_, 2x_, sd_, and other common prefixes.
  - Hash Guessing Lab / Added automatic LCU directory mirroring and modern client Sanctum, Gacha, and Reward Tracker pattern attacks to LCU Basic guessing.
  - Hash Guessing Lab / Expanded LCU Custom guessing with dynamic media (WEBM/OGG) and data (JSON) wordlist suites automatically derived from the known hashes corpus.
  - Hash Guessing Lab / Added the Universal Plugin Modifier attack to LCU Custom, automatically applying Riot UI states, tiers, and layout modifiers across all client plugins.
  - Hash Guessing Lab / Added the Scoped Plugin Engine to LCU Custom, executing intra-plugin directory cross-products and contextual vocabulary substitutions.
  - Hash Guessing Lab / Redesigned interface into a Master-Detail architecture with modular algorithm selection, real-time search filtering, and centralized WPF Themes templates.
  - Comparison Results now include a Results tab showing the export status of each new asset (EXPORTED / FAILED / QUEUED) with batch Extract and Save actions.
  - Comparison Results now offer direct access to the output folder of any exported asset.
  - Added a News section with categorized Riot news feeds, search filtering, and full article reading.
  - News / Added optional background news discovery with its own timer in Advanced settings, notifying through a dedicated NEWS category in the notification hub with click-through to the article.
  - News / Feeds are now limited to the 10 most recent articles per category, reducing transfer and memory usage.
  - News / The feed cache no longer auto-expires; lists are refreshed only on demand (Refresh button or notification click-through).
  - Notifications / The hub's Clear button now removes only the notifications of the active category tab instead of everything.
  - Image Comparison / Timeline Mode: new playable frame sequence that animates the OLD-to-NEW transition with Forward / Round trip shapes, 1-4 repeatable cycles, Play/Pause, Stop, Loop and manual frame scrubbing (Space shortcut).
  - Image Comparison / Timeline Mode: added Wipe and Fade transition effects, selectable in the transport bar.
  - Image Comparison / Timeline Mode: added zoom controls (25%-400% slider, +/- buttons and reset to fit) synchronized with wheel zoom.
  - Image Comparison / Timeline Mode: export the frame sequence as an animated GIF with a configurable hold time per state (2s-5s; the total stays an exact multiple of it - 2x per cycle Forward, (cycles+2)x Round trip - while the short 0.5s wipe/fade is folded into the state boundaries), FPS (12/24/30) and size (1024px, 2048px or original resolution), with Floyd-Steinberg dithering for smoother gradients, following the selected sequence and cycles.
  - Image Comparison / Timeline Mode: OLD/NEW badges identify which image belongs to each side of the transition, both on the viewport and baked into the exported GIF.
  - Core / Centralized the GIF sequence export pipeline (frame sampling, sequence mapping and encoding) under ImageExportUtils.
  - Monitor / Version Manager: Fetch Versions now flags newly discovered manifests with an inline NEW tag (icon + text) next to the publish date.

Fixes
  - Monitor / Mythic Shop now displays portrait and Classic Pass images instead of placeholders.
  - Version Manager now uses the unified 100 ms visual hold between verification and update completion phases.
  - Version downloads now wait for their final progress state before completing verification and update actions.
  - Comparator and Explorer extraction/save now keep their final progress state visible before opening follow-up views.
  - Fixed the first backup run advancing before its final file count was rendered in the status bar.
  - Fixed shared GridView rows rendering diagnostic object type names instead of their authored table columns in VFX Studio.
  - Monitor / Version Manager: Fixed target installation dropdown not updating when switching tabs after creating or cloning a backup.
  - VFX Studio: Fixed billboard particle emitters with opaque RGB masks rendering rectangular background quad artifacts in Alpha and Additive blend modes.
  - Hash Lab: Fixed shader variant guessing to generate low consecutive permutation indices and broaden HLSL path prefixes in Wad Path Grep.

Improvements
  - Monitor / Mythic Shop now resolves images through one catalog lookup covering skins, emotes, wards, icons, portraits, loot, event passes, and nexus finishers.
  - VFX Studio now labels its preview loop control more concisely.
  - VFX Studio now keeps its workspace focused on the viewport, emitters, textures, and meshes while authored ability sequences continue to play automatically.
  - VFX Studio now presents preview looping as a clear on/off transport control that remains active while changing effects.
  - VFX Studio now automatically plays the complete authored event sequence when a selected effect belongs to one unambiguous composition.
  - VFX Studio now builds one authored render queue across root, child, and ability-composed systems, honoring render phase, pass, stable importance, optional emitter depth sorting, and alpha-only output without leaking OpenGL state.
  - VFX Studio now preserves schema-authored emitter defaults and translates every BIN blend mode through explicit RGB/alpha render descriptors; Multiply particles and meshes keep transparent coverage neutral instead of darkening the framebuffer.
  - Hash Guessing Lab / GAME WAD grep now discovers generated dotted BIN paths from their embedded object identifiers.
  - Hash Guessing Lab / GAME Canonical now follows CDTB character templates and exact LCU-to-GAME mappings without duplicate Basic-suite scans.
  - Hash Guessing Lab / GAME Locale Variants now runs GameHashGuesser.SubstituteLang with per-format CheckIter calls matching CDTB.
  - Hash Guessing Lab / GAME Numeric Ranges now uses one GameHashGuesser.SubstituteNumbers call backed by HashGuesser.SubstituteNumbersCore, matching CDTB basename-only number substitution.
  - Hash Guessing Lab / GAME Extended now reports live progress across skin and chroma group bin generations and builds unknown inventories with real-time WAD tracking.
  - Hash Guessing Lab / LCU unknown hash discovery now reads WAD index tables directly without unnecessary image atlas decompression.
  - Hash Guessing Lab / GAME Extended now runs only skin groups, chroma groups, suffixes, skin numbers, character substitution and the GetCharacters corpus helper; custom attacks are reserved for a separate button.
  - Hash Guessing Lab / Banner Guess now uses catalog-attested sports words and bounded compound variants for faster discovery.
  - Hash Guessing Lab / GAME WAD animation links now resolve BIN clip hashes through targeted `.anm` candidates with lower scan time and memory use.
  - Viewer / Project Browser now features a unified context menu on both the tree view and file grid to easily close individual projects or clear the workspace.
  - Viewer / Adjusted default studio lighting to provide realistic 3D shading and highlights when loading models.
  - Viewer / Project Browser file tiles now use the same HoverColor for hover and selection without an extra border overlay.
  - Viewer / Removed the unused WPF lighting objects from the OpenGL viewport; Studio Lighting now has one effective lighting path through the OpenGL renderer.
  - Viewer / Model material effects now classify each BIN-linked submesh conservatively and no longer approximate complex masked vertex deformation as a simple wave.
  - Viewer / Model effects now keep BIN-authored Bloom from overwhelming masked body details.
  - Viewer / Model effects now require an explicit emission signal before applying generic Bloom, preventing gradient/dissolve materials such as Aatrox skin0 from being washed out.
  - Viewer / Model material resolution now recognizes real Scroll/Dissolve/Flow sampler aliases and loads their BIN-referenced dependencies for chroma texture directories.
  - Viewer / Map Environment now selects each map's base visibility layer to prevent overlapping terrain variants.
  - Viewer / Map Environment now renders baked terrain, FlowMap water and vertex-colored foliage correctly.
  - Viewer actions remain responsive during startup and graphics resources are fully released when leaving the view.
  - Viewer / 4K snapshots now suggest a model-based filename automatically.
  - Viewer / Map Environment now reuses mesh metadata, decodes only the required TEX mip and releases temporary renderer memory, reducing usage across repeated large-map loads.
  - Viewer / Fixed background map clipping on large environments and enabled continuous close-up zoom into terrain streets.
  - Standardized identity header position, padding, and corner radius across Hash Guessing Lab and WAD Comparator.
  - Hash Guessing Lab / Added continuous UI timer ticker and offloaded summary count loading to background threads to prevent UI freezes.
  - Monitor / History: reduced the pagination page size of the Comparisons, Watcher and Differences tabs from 10 to 5 entries.
  - Monitor / Backups: replaced the NEWLY CREATED pill label with a minimal inline NEW tag (icon + text) for consistency with the Version Manager list.
  - Monitor / Version Manager: the manifest list now loads ordered by date descending (newest first), replacing the old alphabetical file-name order.
  - Monitor / Version Manager: standardized the download configuration card to match the Asset Tracker hero card (border geometry, icon boxes, title/description typography and optical text alignment).
  - Image Comparison / Wheel zoom now stays within the 25%-400% range and keeps the zoom slider in sync.
  - VFX Studio / Preserved transparent particle backgrounds across color ramps, billboards, trails, rays, and meshes.
  - VFX Studio / Audited real BIN-driven texture alpha, UV multiplier blending, emitter-space placement, and AttachedMesh fallback rendering.
  - VFX Studio / Corrected additive blend handling and alpha clipping while preserving authored texture output.
  - VFX Studio / Recovered legacy opaque RGB masks for billboard-like effects while preserving authored mesh alpha.
  - VFX Studio / Applied authored `modulationFactor` values from VFX BIN emitters to particle and mesh color output with a neutral fallback.
  - VFX Studio / Corrected ground-mesh orientation so authored ±90° ground alignment is not applied twice.
  - VFX Studio / Honored BIN local-orientation flags in both particle basis selection and quad shader geometry.
  - VFX Studio / Applied particle color textures with their authored colorRenderFlags across billboard and mesh emitters.
  - VFX Studio / Applied authored ValueColor animation, palette gradients, and per-particle color selectors from VFX BIN data.
  - VFX Studio / Kept standalone particle meshes double-sided so thin rotating confetti surfaces remain visible.
  - BIN files and comparisons now use the canonical Ritobin structure with linked dependencies, typed properties, and property-level diff filtering; removed the legacy JSON BIN serializer.
  - VFX Studio now follows declared BIN links from the selected skin root, avoiding unrelated sibling or basename matches while preserving truncated extraction collisions.
  - VFX Studio now places the loop boundary at the final particle frame produced by playback.
  - Viewer / Simplified manual map geometry loading to 1-step file dialog using automatic root texture resolution.
  - Viewer / Updated Load Project description to "Explore models & map environments".
  - Viewer / Improved map texture resolution with recursive parent root fallback when loading maps from Project Explorer.
  - Viewer / Supported .mapgeo file selection directly inside Project Explorer browser tree.
  - Viewer / Updated landing descriptions for 3D Workspace hero header, Load Map, and VFX Studio cards.
  - Viewer / Collapsed bottom dock in Map mode to remove obsolete button and empty spacing.
  - VFX Studio / Removed the incomplete AttachedMesh model-binding and rendering path; these emitters remain identified but are skipped until proper owner-model support is implemented.
  - Viewer / Synchronized model visibility toggle in Scene list to enable or hide all submeshes at once.
  - VFX effects now keep child layers aligned and replay with consistent timing and placement.
  - VFX Studio now restores the complete OpenGL render state, isolates animated mesh buffers, skips unnecessary scene captures, honors authored system transforms, and accepts direct vector birth colors from BIN data.
  - VFX Studio now applies consistent BIN blend modes across rendering and diagnostics without exposing black particle backgrounds.
  - VFX Studio / Removed unused animation-event and sound-event parsing from VFX BIN loading.
  - Viewer services and tests are now organized by animation, models, map geometry, rendering, and VFX domains.
  - Viewer Browser now uses the compact shared breadcrumb navigation.
  - Viewer model rendering now reuses animated mesh buffers and shared textures while releasing GPU resources as models are removed.
  - Viewer models now apply supported BIN-authored scrolling and flow effects to the correct submeshes.
  - BIN Hash Lab now preserves observed casing and requires runtime evidence before promoting Meta Schema candidates.
  - BIN Hash Lab now discovers owner-matched entry names without class-specific whitelists.
  - BIN Hash Lab now preserves unpaired semantic matches as research and extracts embedded function names without partial block results.
  - BIN Hash Lab now verifies entry prefixes only against their owning BIN entry and keeps statistically enriched GAME path matches as non-promotable research.
  - BIN type discovery now uses active Meta Schema inheritance and property-type context while preventing class/property cross-domain promotion.
  - Official hash catalogs, locally verified results, research candidates, and legacy overrides now have isolated persistence and resolution provenance.
  - Notification Hub now filters persistent alerts by category with synchronized unread states, actions, and progress feedback.
  - Viewer now offers direct 4K snapshot capture from the viewport toolbar.
  - Viewer now assigns each model submesh from its exact skin BIN, including hash-named PBE metadata, without guessing loadscreen or mask textures.
  - Simplified BIN processing by separating readable JSON output from audio event extraction.
  - Viewer now measures FPS from completed OpenTK render callbacks and applies its optional 60 FPS limit directly in the OpenGL render flow.
  - Viewer keeps high-definition scene capture exclusively in Studio and delegates genuine aspect-preserving UHD framebuffer output to a dedicated OpenGL snapshot service.
  - Viewer viewport, camera, and toolbar-toggle controls now use a more compact layout with smaller icons.
  - Viewer scene ownership now centralizes part attachment, visual synchronization, visibility, and removal through SceneModel and ModelPart.
  - Viewer MapGeometry now composes masked four-layer terrain materials with their authored world-space tiling and RGB blend parameters.
  - Viewer MapGeometry now reads material samplers, texture paths, wrapping, shaders, and color-only materials directly from materials.bin.
  - Viewer image previews now present a clearer icon-led header with the texture name aligned beneath its title.
  - Backups now include integrity manifests, storage preflight, optional names, preferred-client filtering, sorting, and direct folder access.
  - BIN Local GREP now validates identifier hashes only within direct structures or matching map pairs.
  - Enhanced BIN and Lua hash guessing accuracy with strict syntax validation and binary script manifest decoding.
  - Asset Tracker / Increased card margin to 14px for cleaner grid spacing.
  - Manage Versions / Preserved active page selection when refreshing version manifests via Fetch Versions.
  - Viewer Content Browser now keeps multiple opened WAD projects as separate roots and aligns the project toolbar consistently.
  - Viewer project folders now open with a single click in the Content Browser.
  - Increased Hash Guessing Lab left configuration panel width for improved readability.
  - Enabled text wrapping and custom line spacing for the unknown hashes breakdown label.
  - Extended cancellation match preservation to GAME and LCU guessing domains.
  - Version discovery and manifest parsing now use less memory, handle malformed server data safely, and cancel promptly.
  - Game client verification now uses less memory, runs fully in the background, keeps progress responsive, and uses measured CDN concurrency with bounded adaptive bundle ranges.
  - Relocated shader variants, extension substitutions, and prefix attacks to the Game Basic Suite for faster discovery.
  - Increased final progress hold delay from 100ms to 150ms for smoother visual transitions.
  - The main window now opens wider so dashboard status labels remain fully visible.
  - GAME Extended now tests BIN entry basenames against filtered GAME extensions.
  - Hash Guessing progress now presents phase, checked candidates, matches, and speed in one compact line.
  - GAME and LCU guessing suites now reuse the unknown inventory created by WAD Path GREP instead of rescanning WADs.
  - BIN inventory and content scans now discard non-GAME WAD containers centrally while RST scans retain full container coverage.
  - RST content guessing now resolves text sources from both GAME and LCU WAD path indexes.
  - BIN and RST guessing now streams resolved hashes to the results list and preserves matches when a run is cancelled.
  - BIN/RST Hash Lab / Content scanning now certifies literal strings discovered in the file that owns the target hash (OwningFileString), so findings are saved directly into the verified BIN dictionaries instead of staying as research candidates.
  - BIN/RST Hash Lab / Remaining counters now decrease with every finding, sessions only target the current patch (current.*.txt), and RST inventories capture full 64-bit hashes in addition to masked ones.
  - BIN/RST Hash Lab / Content scans are now bounded by a candidate budget and skip XXH hashing when no RST targets remain.
  - BIN/RST Hash Lab / Verified catalogs now reload incrementally after each run instead of reloading every hash catalog, and conflicting FNV1a collisions are excluded from promotion and reported in internal.collisions.json.
  - BIN/RST Hash Lab / Meta Schema unknown class and property hashes are no longer injected into the BIN inventory as guessing targets.
  - BIN/RST Hash Lab / Game path catalog matches now verify directly as BIN entries and hashes instead of statistical research candidates, covering exact paths, extension-stripped paths, parent directories and skin/set sibling enumeration (skin00-49, set01-29, 5M variant budget); exact catalog strings also resolve full XXH64 RST targets while synthetic variants skip truncated 38-bit RST checks to avoid prefix false positives.
  - BIN/RST Hash Lab / Meta Schema wordset, relation and unique exact name matches now verify directly as BIN field and type names instead of research candidates.
  - BIN/RST Hash Lab / Unpaired structural string references (species links, view controller lists and spell data names without an observed hash pair) now verify directly as BIN entries and hashes (SemanticReference) using the same collision guard that reports conflicting FNV1a forms in internal.collisions.json.
  - BIN/RST Hash Lab / Structural guessing now splits known names into attested words (acronym-aware, Hungarian prefixes dropped) and adds four generation passes on top of the existing recombination: Reduction (a known name minus one inner word), Bigram (sequences along word pairs attested in known names, depth 2-4), Attested Tails (top tokens combined with the commonest attested final words) and Word Swap (a known name with one inner word replaced by a corpus word); every pass prints its own expected chance-match count (probes x remaining states / 2^32) to the log, warns when noise could account for half or more of the hits, and preserves attested casing on the candidate names.
  - Hash Guessing / Removed dead code across the guessing services: orphaned candidate generators and attacks (CheckBasenames, RunPrefixAttack, GeneratePrefixAndShaderCandidates, BuildRankedBasenames, BuildRankedDirectoryList), private persistence helpers never called (GetConfirmedResearchPath, WriteHashViewAsync), a never-used splitter regex, and write-only telemetry surface (run UnknownHashesAtStart, inventory Unknowns/PatchFingerprint, progress DiscardedCandidates/CandidatesPerSecond/ManagedMemoryBytes and the matcher discard counter); persisted enum members and test-covered APIs were deliberately kept for research-file compatibility.
  - Hash Guessing / GAME Extended prefix guessing now also covers tft_ prefixes (tft_, 2x_, 2x_sd_, 4x_, 4x_sd_, sd_), restoring the full prefix set that the removed shader/basename generator previously supplied.
  - BIN/RST Hash Lab / The active Meta Schema dictionary (classes and properties, hash plus name) is now merged into the local known catalogs as hashes.metaclasses.txt / hashes.metafields.txt supplements, so BIN types and fields referenced by any bin are resolved by name immediately and never appear as guessing unknowns; verified research matches still take precedence.
  - Shared GAME and LCU basename substitution now reports live candidate counts and speed during exhaustive searches.
  - LCU Basic Suite now caps basename and basename-word substitution at 10M candidates each, and numeric variant guessing only varies digits inside file names (matching CDTB), keeping the suite fast while preserving full-coverage attacks for the Advanced suite.
  - Hash Guessing / GAME number substitution is path-wide again (map11 → map12 folders resolve), while LCU numeric guessing stays anchored to file names for the Basic suite cap.
  - Hash Guessing / GAME skin number substitution now follows CDTB combinations instead of permutations, avoiding the larger permutation cross while preserving mixed skin-token candidates.
  - Hash Guessing / GAME chroma skin groups only run for champions attested in the known corpus (all champions are still processed when the corpus has none), and the character list is cached per hash-file revision.
  - Hash Guessing / GAME basename word and word-addition attacks now share a frequency-ranked wordlist (basename words first) and reuse prebuilt formats cached per hash-file revision, so productive words are tried first and formats are not rebuilt on every run.
  - Hash Guessing / GAME Extended now caps the unbounded attacks (character substitution 10M, basename words 10M, word addition 10M, Cartesian cross 10M, skin numbers 5M) so patches with many unknowns no longer hang, while the frequency-ordered candidates keep the same hit order.
  - Hash Guessing / GAME Extended now runs a single focused BIN word attack with data/ paths first (the previous data-bin attack was a subset of it) and caches the five focused wordlists per hash-file revision.
  - Hash Guessing / GAME local skin groups now enumerate combinations by ascending length up to 8 with a 5M candidate budget (real corpus groups never exceed 8 tokens; TFT pet dragons with 20-50 skins stay reachable via the chroma attack), instead of the unbounded 2^n explosion.
  - Hash Guessing / GAME word-attack formats are now ranked by corpus support (most common path patterns first) so budgets cut the rarest formats instead of the alphabetically last ones, yielding more hits per check.
  - LCU V1 Paths now tests localized variants only when their default path is known, keeping the resolver responsive.
  - LCU V1 Paths now avoids generating locale variants already discoverable from installed WADs.
  - Generated hash paths now use exact matching with shared batch processing and avoid repeated hash-file access.
  - LCU V1 Paths now displays resolved hashes immediately while guessing continues.
  - Hash Guessing Lab now shows session targets, resolved hashes, and clear unresolved-history breakdowns.
  - Automated regression coverage now verifies Viewer scene disposal and media-preview cleanup lifecycles.
  - Hash Guessing now reuses indexed corpus data, reduces temporary buffers across GAME, LCU, BIN and RST, and shows live performance metrics.
  - Image exports now use less memory and remain responsive during encoding.
  - Viewer textures now use less temporary memory while preserving their original quality.
  - Asset Tracker now shares one ten-candidate flow between manual and background checks.
  - Asset Tracker now persists per-ID CDN results and checks both JPG and PNG formats.
  - Simplified GitHub issue templates (Bug Report and Suggestion) to reduce input redundancy and improve user experience.
  - Comparison archives now report when history saving begins.
  - Hash Guessing Lab now shows live candidate counts during long guessing phases.
  - LCU Advanced guessing now targets frontend JSON assets with a dedicated vocabulary.
  - Gallery thumbnails now load in archived results and remain cached while filtering.
  - Explorer previews now distinguish unsupported formats from loading errors while keeping dual previews stable.
  - Media previews now switch faster while keeping transitions free of stale frames.
  - Reduced retained memory by loading WAD content, decoded images, and formatted data only when needed.
  - Asset previews and backups now load WAD content more smoothly.
  - Hash validation now shares the existing benchmark project instead of maintaining a separate test application.
  - LCU Basic guessing now includes full basename and basename-word substitution before its plugin and pattern phases.
  - LCU Advanced guessing now targets two-word party image variants with a reduced PNG vocabulary.
  - GAME chroma-group guessing now works offline from the installed skins catalog.
  - Hash guessing now preserves complete CDTB baseline coverage before running additional optimized GAME and LCU strategies.
  - Hash guessing now uses dedicated shared, GAME, and LCU strategy components with one centralized save flow.
  - Hash guessing now isolates GAME and LCU WAD discovery while preserving shared unknown tracking and expanded path coverage.
  - Hash Guessing Lab results list columns now automatically resize to perfectly fit the window width.
  - Expanded local files scanning to support additional game assets (audio, animations, models, and scripts).
  - BIN Local GREP now collects entry, class, property, and value hashes from inside parsed PROP/PTCH binary trees.
  - Hash Guessing Lab now separates GAME, LCU, BIN, and RST domains with categorized results and patch-aware inventories.
  - BIN and RST inventories now refresh independently and present dedicated actions with consistent strategy icons.
  - BIN guessing now targets only current-patch unknowns while preserving historical inventories for diagnostics.
  - BIN inventory schema changes now invalidate stale markers and trigger one automatic rebuild.
  - Hash guessing now generates wordlists faster with lower memory usage and updates large result sets in one batch.
  - Advanced hash guessing now prioritizes proven Riot path combinations and avoids redundant candidate attacks.
  - Hash Guessing Lab now separates current, recent, and historical unknowns across patches.
  - LCU GREP now resolves relative, CSS, and HTML asset references from their Riot plugin context.
  - GAME and LCU WAD GREP now match CDTB parsing behavior with fewer redundant scans and temporary allocations.
  - GAME and LCU Basic guessing now avoids duplicate candidate passes while preserving CDTB search coverage.
  - GAME Extended now searches base filenames derived from Riot suffix variants.
  - Reduced memory pressure while creating WAD backups and made interrupted saves leave no incomplete history entry.
  - Accelerated Explorer tree loading for large WAD and local folder structures.
  - Improved repeated Explorer searches by reusing the loaded tree index and applying UI updates safely.
  - Redesigned the Hash Guessing Lab interface with a clean Bento Grid layout, real-time statistical tracking of centralized unknowns, and automatic PBE directory configuration from AppSettings.
  - Alternates Bento buttons dynamically between GAME and LCU options panel, optimizing vertical workspace.
  - Added auto-promotion of successfully guessed hashes on execution completion, saving results directly to main hash files.
  - Restructured Home dashboard to use a uniform 4-column Bento grid with a dedicated Riot News tile, giving every card identical sizing.
  - Optimized Home screen text wrapping and card spacing to prevent label clipping across all client modes.
  - Enriched the application navigation, sidebar, and Guessing Lab sections with high-visibility icon sets (cogs, graphs, brains, network).
  - LCU Basic guessing now completes plugin, extension, and built-in pattern searches as independent phases.
  - WAD monitoring and comparisons now use native chunk checksums with faster archive table loading.
  - Audio bank extraction now preserves the original playback/designer order for family groups.
  - Optimized audio event name resolution for champions, skins, and maps by using direct pathing to skip deep recursive searches.
  - Updated vgmstream-cli to the latest version in resources for improved audio decoding.
  - Viewer / Map Geometry Camera: Optimized map geometry (.mapgeo) camera viewport to render an isometric top-down gameplay view at a 50-degree angle and 0.55x distance multiplier, ensuring the entire map is perfectly framed and centered.
  - Explorer / Encrypted Texture Feedback
    * Encrypted Riot esports textures are now identified explicitly in the preview panel instead of being reported as unsupported or failed files.
  - Significantly accelerated hash guessing speed when scanning localized game client containers.
  - Optimized WAD file discovery by searching only inside relevant subfolders (Plugins for LCU, Game for GAME) when they exist.
  - Sound banks (.bnk/.wpk) and standalone audio are now always exported in their original raw format outside the Explorer, preserving byte-for-byte integrity.
  - The global search box now also filters the assets listed in the Comparison Results tab.
  - Export failures no longer stop the whole batch; failed assets can be retried from the Comparison Results tab.
  - Unified the Extract and Save export engine so both act consistently: Extract exports original raw files and Save applies your configured conversions.
  - Manage Versions / Added a Quick Select toggle button for Target Installation to select destination client (MAIN or Backups filtered by preferred environment) when installing client plugins or game files.
  - Comparator / Repositioned file extension badge to top-left and selection checkmark tick to top-right on Results cards for improved visual balance.
  - Notifications / Renamed COMPARATOR tab to TRACKER for dedicated Asset Tracker notifications.
  - Notifications / Removed redundant ISSUES tab from Notification Hub as warnings and errors are already reported in the log console and log files.
  - Home / Standardized every dashboard tile (40px icons, 10.5 labels, 16px titles, 12px descriptions) so all eight cards align perfectly.
  - Home / Status badges (READY / SETUP / MISSING) are now pinned to the right edge of each card header without clipping at any window width.
  - Home / Each card now shows a subtle accent-colored corner watermark of its module that does not affect the layout.
  - Core / PBE maintenance, Automated CDN tracking and background news checks now also run once at application startup, gated by their own Advanced settings and frequencies, so enabled features report updates even when the app is closed before the next timer tick.

Changes
  - Benchmark / Added a MapGeo memory diagnostic that reports WPF geometry, texture, managed and renderer CPU-copy estimates by loading phase.
  - Viewer / Separated scene mesh GPU resources and shared shader source from the main render orchestration.
  - Viewer VFX / Separated texture, scene capture, mesh resource, and GLSL ownership from the particle render orchestration.
  - Image Comparison / Removed the Mathematical Difference Map mode as it became obsolete; the image loading progression now reflects the real remaining steps (acquire 25% - decode 60% - ready 100%).
  - Monitor / Backups: Styled the NEW session snapshot indicator as a HUD technical badge with a 1px border and sparkles icon.
  - Comparator / Renamed internal ComparisonViewMode.Advanced enum value to ComparisonViewMode.Hierarchy for consistency with the UI.

Bug Fixes
  - Fixed transparent VFX layers clipping and flickering against each other when moving the camera.
  - Fixed oversized uniform mesh effects and stretched celebration screens in VFX Studio.
  - Fixed short VFX timelines restarting before the red playhead reached the real end; preview looping now requires the visible gold marker.
  - Viewer VFX Studio / Palette textures now color particles without replacing their authored alpha coverage, fixing opaque colored cards such as `Aatrox_Base_P_Ready`.
  - Viewer VFX Studio / Uniform ground layers now retain circular proportions across their emitters.
  - Viewer / Fixed shared model materials leaking scoped texture effects into unrelated submeshes.
  - Viewer / Close map views no longer disappear behind projection clipping.
  - Viewer now keeps its standard dark background before any model or map is loaded.
  - Viewer / Model, Chroma, and Map Environment loading now stays responsive without cross-thread scene crashes.
  - Viewer / Transparent map textures no longer develop bright borders in generated mipmaps.
  - Viewer / Map textures no longer flicker when viewing large environments from a distance.
  - Explorer / Fixed an issue where navigating away from Local Files in the sidebar could cause an application crash.
  - Monitor / Asset Tracker: the FOUND counter on the CDN Discovery dashboard card now excludes user-removed assets and refreshes immediately after deleting found assets (REMOVE or Clean Found), instead of keeping the stale total until the window was reopened.
  - Viewer / Fixed the Models list layout that was regressed by the file-grid style refactor: model cards now span the full sidebar width in a vertical list with smooth pixel scrolling (restored ModernListBox base style) instead of floating as horizontal wrapped tiles.
  - Comparator / Historical comparison results now re-resolve their hashes when reopened, so assets that were unnamed at comparison time show their real game paths once the hash catalogs are available.
  - Notifications / Fixed notification category classification so Watcher updates, Comparator/PBE status discoveries, and App/Hash updates appear under their respective tabs instead of defaulting to System.
  - Comparator / Fixed export mode labels in results tab to accurately reflect configured protocol settings (Original vs Smart).
  - Explorer / Fixed an issue where selecting specific files in the Grid View would extract or save the entire parent WAD or folder instead of only the selected items.
  - Explorer / Fixed a bug where navigating or selecting a file in the tree would leave stale selections active in the Grid View, incorrectly displaying the floating action bar when returning to the folder.
  - Viewer VFX Studio / Fixed preview startup on GPUs with standard OpenGL vertex attribute limits.
  - Viewer VFX Studio / Restored authored mesh rotations and non-uniform particle dimensions, removing displaced effects and square overlay planes.
  - Viewer VFX Studio / Removed the artificial 8x additive RGB gain so authored spotlight colors and alpha no longer render overexposed.
  - Viewer VFX Studio / Recovered RGB opacity masks from near-opaque compressed alpha channels, removing black particle quads while preserving meaningful transparency.
  - Viewer VFX Studio / Marked AttachedMesh emitters as non-renderable until owner-model binding is available.
  - Viewer VFX Studio / Restored short-lived mesh emitters that must produce their first particle immediately on activation.
  - Viewer VFX Studio / Rebuilt trail segments with authored orientation, longitudinal tiling, sample limits, and cutoff while removing stationary rectangular artifacts.
  - Viewer VFX Studio / Removed white particle squares caused by additive color and multiplier textures overriding authored transparency.
  - Viewer VFX Studio / Preserved authored SCB/SCO vertex-color gradients so mesh walls and ground layers retain their intended tint and vertical transparency fades.
  - Viewer VFX Studio / Restored authored arbitrary-quad diameters so expanding rings reach their matching mesh boundaries before fading.
  - Viewer VFX Studio / Fixed rotated circular particle masks rendering as repeated corner fragments or stretched quads by honoring transparent texture borders and authored erosion defaults.
  - Viewer VFX Studio / Restored radial particle waves by rotating Legacy spawn motion with its authored emission angle and preserving direction-oriented arbitrary-quad planes.
  - Viewer VFX Studio / Fixed emitter visibility checkboxes in the timeline so their full shape remains visible inside compact rows.
  - Viewer VFX Studio / The yellow loop boundary now follows natural particle and child-system completion instead of treating externally triggered particle/emitter linger values as extra playback time.
  - Viewer VFX Studio / Prevented particleColorTexture-only emitters from rendering the opaque white fallback quad, and exposed base, multiplier, and color-lookup texture roles in Emitters Breakdown.
  - Viewer VFX Studio / Corrected ground-layer arbitrary quads authored with a downward-facing basis so their visual orientation faces upward and follows the expected 2D direction.
  - Viewer chromas now use their own skin BIN so every mesh receives the correct variant texture.
  - Viewer camera toolbar now switches reliably between exact front, back, side, top, and bottom views.
  - Viewer now resolves companion theme materials through their exact skin BIN, including textures referenced from external skin folders.
  - Explorer now clears Grid range selection and its anchor before opening a file.
  - VFX Studio now excludes authored-disabled systems and emitters from playback counts and timeline duration.
  - VFX Studio now presents Riot's authored forward direction toward the bottom of the viewport.
  - Viewer now renders Riot models that use submesh-relative vertex indices correctly.
  - Comparator now clears stale Base and Target data when the preferred game client or its directory changes.
  - Comparator now refreshes Quick Select and version metadata whenever the view is reopened after a game update.
  - Comparator Quick Select now shows only the preferred PBE or LIVE environment while preserving MAIN and backup entries.
  - Restored the original height and rounded proportions of the Comparison Results search and filter toolbar.
  - Preserved Comparator selections and path fields when navigating between main views.
  - Restored resolved audio event names in archived comparisons by preserving dependency decompression metadata and supporting legacy Zstd chunks.
  - Comparator / Smart Sync now mirrors the selected sub-directory (e.g. Plugins) into the base backup path, both on automatic pairing and when switching backups via Quick Select.
  - Comparator / Clearing the Target directory now resets the whole sync pair (Base included), so picking a backup afterwards pairs the roots instead of the previous sub-directory.
  - News / Article banner images now fit inside the rounded header panel instead of overflowing its corners.
  - News / Background discovery now notifies every article published after the newest one already seen, with no per-run cap: the seen-state reference date keeps old feed rotation from ever being re-notified, while all genuinely new articles are reported.
  - News / The seen-state cache now stores publish dates alongside article URLs (auto-migrated from the legacy format) and keeps entries synchronized with the current feed so nothing still published can ever be re-notified.
  - News / Marking a news notification as read (or opening it) now marks the article as seen, so it is never notified again even across app restarts; opening an article from the feed does the same.
  - News / Notification hub cards now use a dedicated newspaper icon and show the article's publish time (relative) instead of the generic notification time.
  - News / Feed toolbar refresh and sort buttons now share the app's standard transparent icon style (as used in LogView), grouped side by side with a divider, and the search box is taller (36px, matching the category picker) and more compact (190px).
  - News / Article reader now shows the lead description, a subtle divider, and the article authors (from the article page) next to the publish date.
  - News / Article loading now shows a skeleton card (banner, title and body lines with a soft pulse animation) plus the spinner, instead of a bare mini spinner.
  - News / Feed toolbar now offers content type filtering (All / Articles / Videos) and a newest/oldest sort toggle, combining with the text search for filtered result counts and a dedicated no-results state.
  - Preserved PTCH data overrides in BIN previews and comparisons.
  - Fixed WAD path normalization and BIN name resolution across class, property, entry, and value domains.
  - Viewer maximize mode now spans the viewport across the full main-content width instead of leaving the hidden inspector column reserved.
  - Viewer Studio snapshots no longer capture the zero-sized legacy camera host or upscale the visible viewport framebuffer.
  - Viewer MapGeometry now resolves Jade textures without false missing warnings, filename collisions, or camera-dependent texture tiling artifacts.
  - Viewer MapGeometry now selects loaded maps in the inspector and keeps large scenes inside the visible camera range.
  - Viewer MapGeometry now opens without the studio grid covering the map.
  - VFX Studio now supports close camera zoom while preserving its dedicated framing and reset position.
  - Backup snapshot count and storage metrics now follow the selected LIVE, PBE, or ALL filter.
  - Unique Meta Schema resolutions now move from research into BIN type and field overrides.
  - Verified RST overrides now resolve across string tables immediately and after restarting the app.
  - MAIN game installations remain selectable for cloning but cannot be deleted as backup snapshots.
  - BIN Hash Guessing now rejects self-confirming literals and cross-domain evidence.
  - BIN Hash Guessing now separates review candidates from context-verified overrides and quarantines legacy findings.
  - BIN inventories now include active unresolved Meta Schema types and fields.
  - Viewer VFX Studio / Rebuilt skin BIN dependency loading so inherited and extractor-truncated effects load without omissions or cross-skin matches.
  - Viewer VFX Studio / Fixed texture colors, transparency modes, atlas frames, UV transforms, erosion, reflection, distortion, and soft-particle intersections.
  - Viewer VFX Studio / Fixed global placement, scaling, and AttachedMesh overlays so VFX follow the animated champion without creating a duplicate body.
  - Viewer VFX Studio / Standardized the systems search field with the shared search-box style, restoring the visible white text caret, placeholder, focus state, and clear action.
  - Viewer VFX Studio / Fixed invisible ground-layer effects such as `Locke_Base_E_Dash_Trail_child_splash` by preventing soft-particle depth fading from erasing geometry coincident with the ground.
  - Viewer VFX Studio / Removed the premature empty-frame restart so delayed, low-rate, and event-driven emitters can reach their authored trigger time.
  - Viewer VFX Studio / Synchronized the red playback needle with the yellow finite-duration boundary; particle exhaustion no longer restarts a finite effect before its authored timeline ends.
  - Viewer VFX Studio / Corrected delayed-emitter timeline geometry so each colored bar starts at its yellow emission marker and ends after emission plus residual particle lifetime.
  - Viewer VFX Studio / Corrected authored `Ray` and `Beam` orientation by using their local forward axis and avoiding duplicate 2D rotation, fixing displaced spotlights such as Samira Skin 30 Pentakill.
  - Viewer VFX Studio / Made `Ray` and `Beam` width camera-facing while preserving authored length direction, restoring both broad Samira Pentakill spotlights instead of edge-on or overlapping planes.
  - Viewer VFX Studio / Preserved initial mesh rotation and rotational velocity on all three axes so authored particle spin is reproduced completely.
  - Viewer VFX Studio / Applied authored uniform birth scaling to mesh primitives, preventing celebration props such as Samira's jumbotron screens and body from expanding by unused Y/Z scale channels.
  - Viewer VFX Studio / Respected uniform lifetime scaling so animated mesh confetti retains its complete 3D shape instead of collapsing into colored streaks.
  - Viewer VFX Studio / Applied uniform birth sizing to billboard flashes without overwriting mesh proportions, restoring compact sparkles instead of elongated color stains.
  - Viewer VFX Studio / Extended downward spotlights to their nearby ground intersection so authored impact lights and beams meet without a floating gap.
  - Viewer VFX Studio / Matched Rays to direction-aligned impact meshes so crossing spotlights continue into their authored opposite-side ground lights.
  - Viewer VFX Studio / Fixed Z-scale flattening bug for 3D Mesh Emitters to prevent 0.01f paper-thin mesh scaling.
  - Viewer VFX Studio / Fixed razor-thin 1.5px quad particle scaling in Sett_Base_W_Min_Swipe by correctly evaluating scalar aspect ratio multipliers.
  - Viewer VFX Studio / Fixed UI clipping for SpeedComboBox (0.25x) and top golden LoopBoundaryHandle shadow.
  - Viewer VFX Studio / Fixed Z-index order and hit-testing priority for golden LoopBoundaryHandle to prevent accidental playhead dragging.
  - Viewer VFX Studio / Made golden loop boundary needle interactively draggable with mouse, and corrected IsSingleParticle lifespan detection (e.g. 0.31s for Sett_Base_Q_Swipe_B instead of 5.30s).
  - Viewer VFX Studio / Added golden loop boundary needle with circular handle (matching Riot editor UI design) to visually bound active VFX duration and prevent timeline jumps.
  - Viewer VFX Studio / Preserved authored zero scale axes used by planar mesh effects instead of replacing them with unrelated dimensions.
  - Viewer VFX Studio / Fixed playback for Sett_Base_W_Min_Tar_Center and short target hit impacts with seamless auto-restart looping.
  - Viewer VFX Studio / Fixed playback and rendering for fast impulse hit effects (like Sett_Base_BA_Hit_01) by allowing fallback texture rendering and accurate short-duration timeline calculation.
  - Viewer VFX Studio / Fixed duplicate skin entries in skin selector by filtering strictly for BIN files inside /skins/ folder.
  - Viewer VFX Studio / Fixed sub-UV texture atlas grid scaling (TexDiv) for mesh emitters to eliminate tiled square particle artifacts.
  - Viewer VFX Studio / Fixed stencil mask handling (negative RenderPass) to eliminate black stencil cap box mesh artifacts in particle craters.
  - Viewer VFX Studio / Fixed blend mode handling for additive particle emitters (blendMode 4 & 5) to eliminate black rectangular overlays in ground effects.
  - Viewer VFX Studio / Unified viewport camera controls, panning, smooth interpolation, and zoom modifiers with the 3D Model Viewport.
  - Viewer VFX Studio / Fixed texture masking on particles to eliminate dark rectangular overlays.
  - Viewer VFX Studio / Fixed texture transparency rendering for Alpha Blend (blendMode 0) particles to eliminate black borders, solid background glows, and unhandled texture alpha artifacts.
  - Viewer VFX textures now animate their authored UV movement, rotation, and multiplier transforms throughout particle playback.
  - Viewer VFX systems can now be selected without crashing the playback panel.
  - Viewer VFX now evaluates authored birth curves, trail geometry, UV scrolling, and erosion channels correctly.
  - Removed synthetic BIN path reconstruction, cross-dictionary structural attacks, and unrelated JSON, CSS, JavaScript, text, and executable scans from BIN guessing.
  - BIN guessing now rejects same-file coincidences without a proven structural relationship and automatically purges legacy unverified research records.
  - BIN inventory now classifies nested ObjectLink values as BIN Entries.
  - BIN JSON now keeps fields, types, entries, and hash values isolated so cross-domain collisions cannot produce incorrect names.
  - Game updates now reject corrupt server chunks and repair incorrect trailing file data safely.
  - Extensionless UI atlas assets now export with their correct `.atlas` extension.
  - GAME and LCU hash files now preserve CommunityDragon-compatible LF line endings when new matches are saved.
  - Version updates now display the completed verification and updating states before advancing or closing.
  - Background update, Asset Tracker, and PBE checks no longer overlap or continue notifying after shutdown.
  - Raw export now correctly saves loose physical files, including local BNK and WPK sound banks.
  - WAD Path GREP no longer runs the numbered shader attack reserved for GAME Basic Suite.
  - Comparison results no longer report renamed assets as removed.
  - Prevented temporary CDN errors from being recorded as permanently missing assets.
  - Fixed selected Explorer and comparison results keeping their NEW, MODIFIED, and REMOVED colors.
  - Fixed archived single-WAD comparisons saving empty snapshots that could not be previewed.
  - Fixed new asset extraction from a single WAD comparison using invalid duplicate WAD paths.
  - Explorer SAVE now applies SMART conversion to complete selected folders independently of asset-extraction preferences.
  - Fixed Comparison Gallery memory growth, recycled thumbnail loading, and SVG previews after closing or filtering results.
  - Fixed archived Zstd chunk previews without requiring auxiliary metadata.
  - Fixed Versions list scrolling when the pointer is over a version entry.
  - Fixed and unified file selection across Explorer trees, grids, and comparison results while preventing cancelled preview errors.
  - Fixed cancelled media previews removing files used by the active preview.
  - Fixed transparent Viewer backgrounds not accepting camera controls outside the model.
  - Fixed Viewer studio settings and environment options not resetting after removing the last model.
  - Fixed the ground plane reappearing after loading a new model when it was disabled.
  - Fixed previews changing unexpectedly when switching files quickly.
  - Fixed individual WAD lookups failing when virtual paths use backslashes.
  - Fixed ModernListView selected and hover state colors to consistently use HoverColor in Hash Guessing Lab results.
  - Fixed progress spinner animation continuing to rotate after search completes.
  - Fixed BIN and RST guessing results displaying fully qualified class names instead of resolved paths.
  - Fixed source WAD files not displaying correct container paths for resolved BIN and RST matches.
  - Fixed BIN unknown inventories showing 64-bit padding and retaining placeholder zero hashes.
  - Fixed LCU embedded paths losing their directory context during guessing.
  - Added working cancellation, accurate indeterminate progress, and safer unknown-hash persistence to Hash Guessing Lab.
  - Fixed guessing suites reintroducing hashes resolved by an earlier phase of the same run.
  - Fixed historical GAME paths with repeated separators and reduced invalid LCU translation guesses.
  - Fixed recycled Explorer rows changing previews, tabs, and pinned files while scrolling.
  - Fixed BIN comparisons falling back to full JSON when files contain empty optional values.
  - Fixed extraction failing on WADs containing extensionless atlas files that share their name with a sibling directory.
  - Fixed duplicate unsupported format preview screens appearing when clearing the search filter.
  - Fixed application crash when opening the Hash Guessing Lab due to a missing button style or duplicate column definitions.
  - Fixed UI freezing (Not Responding) during long hash guessing sessions by executing calculations asynchronously in background threads.
  - Fixed LCU hash guessing executing redundant search operations on non-text binary files.
  - Fixed Cancel button styling in the Hash Guessing window to match the secondary button design.
  - Fixed WAD Grep UI progress freezes on massive LCU WAD files by updating progress periodically inside the chunk loop.
  - Fixed 3D viewer model rendering as completely white when loading models containing custom file extensions.
  - Fixed a texture mismatch issue where models could load incorrect textures (such as Ahri textures on Locke) due to dictionary property ordering.
  - Removed legacy static mesh loader (ScoLoadingService) and its associated file formats (.sco and .scb) to clean up the codebase.
  - Home / Fixed PBE, LIVE and LOCAL card descriptions rendering lower than the rest of the grid because the background watermark inflated the tile layout.
