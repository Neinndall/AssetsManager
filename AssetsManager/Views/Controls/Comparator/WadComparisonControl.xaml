<UserControl x:Class="AssetsManager.Views.Controls.Comparator.WadComparisonControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AssetsManager.Views.Controls.Comparator"
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- 1. Header & Mode -->
            <RowDefinition Height="*"/>    <!-- 2. Dual Comparison Cards -->
            <RowDefinition Height="Auto"/> <!-- 3. Action Footer -->
        </Grid.RowDefinitions>

        <!-- 1. HEADER: Mode Switcher -->
        <Border Grid.Row="0" Margin="0,0,0,24"
                Background="{StaticResource CardBackground}" 
                BorderBrush="{StaticResource BorderColor}" 
                BorderThickness="1" 
                CornerRadius="12" 
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Title & Context -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource HoverColor}" CornerRadius="8" Width="38" Height="38" Margin="0,0,12,0">
                        <materialIcons:MaterialIcon Kind="Wrench" Foreground="{StaticResource AccentBrush}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Configuration" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Select a mode and set up your comparison by selecting base and target sources." FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,1,0,0"/>
                    </StackPanel>
                </StackPanel>

                <!-- Mode Selector -->
                <Border Grid.Column="1" Background="{StaticResource SidebarBackground}" CornerRadius="8" Padding="4" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="ModeDirectory" Content="DIRECTORY" GroupName="ComparisonMode" IsChecked="{Binding IsDirectoryMode}" Style="{StaticResource SegmentedRadioButtonStyle}"/>
                        <RadioButton x:Name="ModeFile" Content="WAD FILE" GroupName="ComparisonMode" IsChecked="{Binding IsFileMode, Mode=OneWay}" Style="{StaticResource SegmentedRadioButtonStyle}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- 2. DUAL CARDS -->
        <Grid Grid.Row="1" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="64"/> <!-- Divider Gap -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT CARD: TARGET VERSION (NEW) -->
            <Border Grid.Column="0" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="16" Padding="28">
                <StackPanel>
                    <!-- Header -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                        <Border Background="#1A00E676" CornerRadius="8" Width="36" Height="36" Margin="0,0,14,0">
                            <materialIcons:MaterialIcon Kind="AutoFix" Foreground="{StaticResource AccentGreen}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="TARGET VERSION" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentGreen}"/>
                            <TextBlock Text="New source to compare with" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Quick Select Dropdown -->
                    <TextBlock Text="QUICK SELECT" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="4,0,0,8"/>
                    <ComboBox x:Name="TargetQuickSelect" 
                              Style="{StaticResource ModernComboBox}" 
                              Height="42" 
                              ItemsSource="{Binding AvailableBackups}"
                              SelectionChanged="TargetQuickSelect_SelectionChanged"
                              Margin="0,0,0,16">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <materialIcons:MaterialIcon Grid.Column="0" Kind="FolderZipOutline" Width="16" Height="16" Foreground="{StaticResource TextMuted}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding DisplayName}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                    
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="12,0" VerticalAlignment="Center">
                                        <materialIcons:MaterialIcon Kind="Calendar" Width="12" Height="12" Foreground="{StaticResource TextMuted}" Margin="0,0,4,0"/>
                                        <TextBlock Text="{Binding CreationDate, StringFormat={}{0:dd/MM/yyyy HH:mm}}" FontSize="10" Foreground="{StaticResource TextSecondary}"/>
                                    </StackPanel>

                                    <Border Grid.Column="3" Background="{StaticResource HoverColor}" CornerRadius="4" Padding="6,2" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Version}" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <!-- Manual Path Row -->
                    <TextBlock Text="MANUAL PATH" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="4,0,0,8"/>
                    <Grid Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Height="38" IsReadOnly="True" Padding="12,0" VerticalContentAlignment="Center" Margin="0,0,8,0">
                            <TextBox.Style>
                                <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBox}">
                                    <Setter Property="Text" Value="{Binding NewDirectoryPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsFileMode}" Value="True">
                                            <Setter Property="Text" Value="{Binding NewWadFilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                        <Button Grid.Column="1" Style="{StaticResource BrowseButtonStyle}" Width="38" Height="38" Padding="0" 
                                Click="btnSelectNewLolPbeDirectory_Click" 
                                Visibility="{Binding IsDirectoryMode, Converter={StaticResource BoolToVisibilityConverter}}">
                            <materialIcons:MaterialIcon Kind="FolderOpen" Width="18"/>
                        </Button>
                        <Button Grid.Column="1" Style="{StaticResource BrowseButtonStyle}" Width="38" Height="38" Padding="0" 
                                Click="btnSelectNewWadFile_Click" 
                                Visibility="{Binding IsFileMode, Converter={StaticResource BoolToVisibilityConverter}}">
                            <materialIcons:MaterialIcon Kind="FileFind" Width="18"/>
                        </Button>
                    </Grid>

                    <!-- Metadata HUD -->
                    <Border Background="{StaticResource SidebarBackground}" CornerRadius="12" Padding="16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="VERSION" FontSize="9" FontWeight="Black" Foreground="{StaticResource TextMuted}" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding TargetVersion}" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                <TextBlock Text="CLIENT TYPE" FontSize="9" FontWeight="Black" Foreground="{StaticResource TextMuted}" Margin="0,0,0,4" HorizontalAlignment="Right"/>
                                <StackPanel Orientation="Horizontal">
                                    <Border CornerRadius="4" Padding="6,2" Background="{StaticResource HoverColor}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Margin="0,0,8,0">
                                        <TextBlock FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextPrimary}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="LIVE"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding TargetVersion}" Value="---">
                                                            <Setter Property="Text" Value="---"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsTargetPbe}" Value="True">
                                                            <Setter Property="Text" Value="PBE"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                    <Border CornerRadius="4" Padding="6,2" Background="{StaticResource AccentBrush}" Visibility="{Binding IsTargetActive, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock Text="ACTIVE" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                    <Border CornerRadius="4" Padding="6,2" Background="{StaticResource HoverColor}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsTargetActive}" Value="False"/>
                                                            <Condition Binding="{Binding TargetVersion, Converter={StaticResource EqualityConverter}, ConverterParameter='!---'}" Value="True"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="BACKUP" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextSecondary}"/>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>

            <!-- CENTER ICON -->
            <Grid Grid.Column="1" VerticalAlignment="Center">
                <Border Width="48" Height="48" CornerRadius="24" Background="{StaticResource SidebarBackground}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                    <materialIcons:MaterialIcon Kind="SwapHorizontal" Foreground="{StaticResource TextMuted}" Width="28" Height="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </Grid>

            <!-- RIGHT CARD: BASE VERSION (OLD) -->
            <Border Grid.Column="2" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="16" Padding="28">
                <StackPanel>
                    <!-- Header -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                        <Border Background="#1AFF9100" CornerRadius="8" Width="36" Height="36" Margin="0,0,14,0">
                            <materialIcons:MaterialIcon Kind="History" Foreground="{StaticResource AccentOrange}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="BASE VERSION" FontSize="12" FontWeight="Black" Foreground="{StaticResource AccentOrange}"/>
                            <TextBlock Text="Old source to compare against" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Quick Select Dropdown -->
                    <TextBlock Text="QUICK SELECT" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="4,0,0,8"/>
                    <ComboBox x:Name="BaseQuickSelect" 
                              Style="{StaticResource ModernComboBox}" 
                              Height="42" 
                              ItemsSource="{Binding AvailableBackups}"
                              SelectionChanged="BaseQuickSelect_SelectionChanged"
                              Margin="0,0,0,16">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <materialIcons:MaterialIcon Grid.Column="0" Kind="FolderZipOutline" Width="16" Height="16" Foreground="{StaticResource TextMuted}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding DisplayName}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                    
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="12,0" VerticalAlignment="Center">
                                        <materialIcons:MaterialIcon Kind="Calendar" Width="12" Height="12" Foreground="{StaticResource TextMuted}" Margin="0,0,4,0"/>
                                        <TextBlock Text="{Binding CreationDate, StringFormat={}{0:dd/MM/yyyy HH:mm}}" FontSize="10" Foreground="{StaticResource TextSecondary}"/>
                                    </StackPanel>

                                    <Border Grid.Column="3" Background="{StaticResource HoverColor}" CornerRadius="4" Padding="6,2" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Version}" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <!-- Manual Path Row -->
                    <TextBlock Text="MANUAL PATH" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="4,0,0,8"/>
                    <Grid Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Height="38" IsReadOnly="True" Padding="12,0" VerticalContentAlignment="Center" Margin="0,0,8,0">
                            <TextBox.Style>
                                <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBox}">
                                    <Setter Property="Text" Value="{Binding OldDirectoryPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsFileMode}" Value="True">
                                            <Setter Property="Text" Value="{Binding OldWadFilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                        <Button Grid.Column="1" Style="{StaticResource BrowseButtonStyle}" Width="38" Height="38" Padding="0" 
                                Click="btnSelectOldLolPbeDirectory_Click" 
                                Visibility="{Binding IsDirectoryMode, Converter={StaticResource BoolToVisibilityConverter}}">
                            <materialIcons:MaterialIcon Kind="FolderOpen" Width="18"/>
                        </Button>
                        <Button Grid.Column="1" Style="{StaticResource BrowseButtonStyle}" Width="38" Height="38" Padding="0" 
                                Click="btnSelectOldWadFile_Click" 
                                Visibility="{Binding IsFileMode, Converter={StaticResource BoolToVisibilityConverter}}">
                            <materialIcons:MaterialIcon Kind="FileFind" Width="18"/>
                        </Button>
                    </Grid>

                    <!-- Metadata HUD -->
                    <Border Background="{StaticResource SidebarBackground}" CornerRadius="12" Padding="16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="VERSION" FontSize="9" FontWeight="Black" Foreground="{StaticResource TextMuted}" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding BaseVersion}" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                <TextBlock Text="CLIENT TYPE" FontSize="9" FontWeight="Black" Foreground="{StaticResource TextMuted}" Margin="0,0,0,4" HorizontalAlignment="Right"/>
                                <StackPanel Orientation="Horizontal">
                                    <Border CornerRadius="4" Padding="6,2" Background="{StaticResource HoverColor}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Margin="0,0,8,0">
                                        <TextBlock FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextPrimary}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="LIVE"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding BaseVersion}" Value="---">
                                                            <Setter Property="Text" Value="---"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsBasePbe}" Value="True">
                                                            <Setter Property="Text" Value="PBE"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                    <Border CornerRadius="4" Padding="6,2" Background="{StaticResource AccentBrush}" Visibility="{Binding IsBaseActive, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock Text="ACTIVE" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                    <Border CornerRadius="4" Padding="6,2" Background="{StaticResource HoverColor}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsBaseActive}" Value="False"/>
                                                            <Condition Binding="{Binding BaseVersion, Converter={StaticResource EqualityConverter}, ConverterParameter='!---'}" Value="True"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="BACKUP" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextSecondary}"/>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <!-- 3. FOOTER: Start Comparison -->
        <Border Grid.Row="2" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="12" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{StaticResource HoverColor}" CornerRadius="10" Width="38" Height="38" Margin="0,0,12,0">
                    <materialIcons:MaterialIcon Kind="CheckAll" Foreground="{StaticResource AccentBrush}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Ready to Analyze?" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="The comparison will identify new, modified, renamed and deleted assets." FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                </StackPanel>

                <Button Grid.Column="2" x:Name="compareWadButton" Click="compareWadButton_Click" IsEnabled="{Binding IsComparing, Converter={StaticResource InverseBooleanConverter}}" 
                        Style="{StaticResource PrimaryButton}" Padding="10,0,14,0">
                    <StackPanel Orientation="Horizontal">
                        <materialIcons:MaterialIcon Kind="Flash" Margin="0,0,8,0" Width="14" Height="14" VerticalAlignment="Center"/>
                        <TextBlock Text="START COMPARISON" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>