<UserControl x:Class="AssetsManager.Views.Controls.Monitor.FileWatcherControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AssetsManager.Views.Controls.Monitor"
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">

    <Grid Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Toolbar -->
            <RowDefinition Height="*"/>    <!-- List -->
        </Grid.RowDefinitions>

        <!-- 1. HEADER -->
        <Border Grid.Row="0" Margin="0,0,0,24" Background="{StaticResource CardBackground}" CornerRadius="12" Padding="24,20" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Background="{StaticResource HoverColor}" CornerRadius="10" Width="50" Height="50" Margin="0,0,20,0">
                    <materialIcons:MaterialIcon Kind="EyeSettingsOutline" Width="26" Height="26" Foreground="{StaticResource AccentBlue}"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="File Watcher" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="Live monitoring of remote data files for instant change detection" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 2. TOOLBAR -->
        <Border Grid.Row="1" Margin="0,0,0,16" Background="{StaticResource SidebarBackground}" CornerRadius="8" Padding="12" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Click="AddUrl_Click" Style="{StaticResource ModernButton}" HorizontalContentAlignment="Left" Padding="12,0,16,0">
                    <StackPanel Orientation="Horizontal">
                        <materialIcons:MaterialIcon Kind="Plus" Width="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Add File" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <TextBox Grid.Column="2" x:Name="txtSearch" Style="{StaticResource StandardSearchBoxStyle}" Width="240" Height="36" TextChanged="TxtSearch_TextChanged" Tag="Search monitors..."/>
            </Grid>
        </Border>

        <!-- 3. MONITORS LIST -->
        <ListView x:Name="MonitoredItemsListView" 
                  Grid.Row="2" 
                  ItemsSource="{Binding MonitoredItems}" 
                  Style="{StaticResource ModernListView}"
                  Background="Transparent" BorderThickness="0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Margin" Value="0,0,0,8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource CardBackground}" CornerRadius="12" Padding="0" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                        <Grid>
                            <!-- Accent Strip (Left) -->
                            <Border Width="4" HorizontalAlignment="Left" CornerRadius="12,0,0,12">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource BorderColor}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding StatusText}" Value="Updated">
                                                <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding StatusText}" Value="Up-to-date">
                                                <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding StatusText}" Value="Error">
                                                <Setter Property="Background" Value="{StaticResource AccentRed}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>

                            <Grid Margin="16,16,16,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/> <!-- Status Icon -->
                                    <ColumnDefinition Width="*"/>    <!-- Info -->
                                    <ColumnDefinition Width="Auto"/> <!-- Actions -->
                                </Grid.ColumnDefinitions>

                                <!-- Status Indicator Box -->
                                <Border Grid.Column="0" Width="44" Height="44" CornerRadius="10" Background="{StaticResource HoverColor}" Margin="4,0,20,0">
                                    <materialIcons:MaterialIcon Width="22" Height="22" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <materialIcons:MaterialIcon.Style>
                                            <Style TargetType="materialIcons:MaterialIcon">
                                                <Setter Property="Kind" Value="HelpCircleOutline" />
                                                <Setter Property="Foreground" Value="{StaticResource TextMuted}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding StatusText}" Value="Up-to-date">
                                                        <Setter Property="Kind" Value="CheckCircleOutline" />
                                                        <Setter Property="Foreground" Value="{StaticResource AccentGreen}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding StatusText}" Value="Updated">
                                                        <Setter Property="Kind" Value="BellRingOutline" />
                                                        <Setter Property="Foreground" Value="{StaticResource AccentBlue}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding StatusText}" Value="Checking...">
                                                        <Setter Property="Kind" Value="Sync" />
                                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding StatusText}" Value="Error">
                                                        <Setter Property="Kind" Value="AlertCircleOutline" />
                                                        <Setter Property="Foreground" Value="{StaticResource AccentRed}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </materialIcons:MaterialIcon.Style>
                                    </materialIcons:MaterialIcon>
                                </Border>

                                <!-- Info -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Alias}" FontWeight="Bold" FontSize="15" Foreground="{StaticResource TextPrimary}"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <Border Background="{StaticResource SidebarBackground}" CornerRadius="4" Padding="8,2" Margin="0,0,12,0">
                                            <TextBlock Text="{Binding StatusText}" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextMuted}"/>
                                        </Border>
                                        <TextBlock Text="|" FontSize="11" Foreground="{StaticResource BorderColor}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        <TextBlock Text="{Binding LastChecked}" FontSize="11" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Actions -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                                                         <!-- Compare Action -->
                                                                        <Button Click="ViewChanges_Click" ToolTip="View Differences" Margin="0,0,8,0">
                                                                            <Button.Style>
                                                                                <Style TargetType="Button" BasedOn="{StaticResource ModernButton}">
                                                                                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                                                                    <Setter Property="Padding" Value="12,0,16,0"/>
                                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                                    <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding HasChanges}" Value="True">
                                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                                        </DataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </Button.Style>
                                                                            <StackPanel Orientation="Horizontal">
                                                                                <materialIcons:MaterialIcon Kind="FileCompare" Width="14" Margin="0,0,8,0"/>
                                                                                <TextBlock Text="Compare" FontSize="11" FontWeight="Bold"/>
                                                                            </StackPanel>
                                                                        </Button>                                    
                                    <!-- Remove Action -->
                                    <Button Click="Remove_Click" Style="{StaticResource TransparentIconButton}" Width="32" Height="32" ToolTip="Remove Monitor">
                                        <materialIcons:MaterialIcon Kind="TrashCanOutline" Width="16" Foreground="{StaticResource AccentRed}"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>