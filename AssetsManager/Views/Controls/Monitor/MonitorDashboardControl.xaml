<UserControl x:Class="AssetsManager.Views.Controls.Monitor.MonitorDashboardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="900">

    <Grid Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header: Main Status -->
            <RowDefinition Height="Auto"/> <!-- Service Grid -->
        </Grid.RowDefinitions>

        <!-- 1. HEADER: Main Status + HUD -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="{StaticResource HoverColor}" CornerRadius="8" Width="32" Height="32" Margin="0,0,12,0">
                    <materialIcons:MaterialIcon Kind="StarFourPoints" Width="16" Height="16" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="MAIN STATUS" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Right: Status Indicators -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <!-- PBE Status -->
                <Border Background="{StaticResource SidebarBackground}" Padding="12,6" CornerRadius="8" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="8" Height="8" Fill="{Binding PbeStatusColor}" Margin="0,0,8,0" VerticalAlignment="Center">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="6" ShadowDepth="0" Color="{Binding PbeStatusColor.Color}" Opacity="0.6"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <TextBlock Text="PBE ENVIRONMENT" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Data Status -->
                <Border Background="{StaticResource SidebarBackground}" Padding="12,6" CornerRadius="8" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="8" Height="8" Fill="{Binding DataStatusColor}" Margin="0,0,8,0" VerticalAlignment="Center">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="6" ShadowDepth="0" Color="{Binding DataStatusColor.Color}" Opacity="0.6"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <TextBlock Text="DATA INTEGRITY" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Core Status -->
                <Border Background="{StaticResource SidebarBackground}" Padding="12,6" CornerRadius="8">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="8" Height="8" Fill="{Binding AppVersionColor}" Margin="0,0,8,0" VerticalAlignment="Center">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="6" ShadowDepth="0" Color="{Binding AppVersionColor.Color}" Opacity="0.6"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <TextBlock Text="SYSTEM CORE" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- 2. SERVICE GRID: Reorganized Layout (Single Row HUD) -->
        <UniformGrid Grid.Row="1" Columns="4">
            
            <!-- CARD 1 (Left): PBE SERVER -->
            <Border Background="{StaticResource CardBackground}" CornerRadius="12" Margin="0,0,6,0" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Padding="16,15" MinHeight="220">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                        <materialIcons:MaterialIcon Kind="ServerNetwork" Width="18" Height="18" Foreground="{StaticResource AccentBlue}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="PBE SERVER" FontWeight="Bold" FontSize="12" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- Main Content -->
                    <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="0,0,0,12">
                        <TextBlock Text="{Binding PbeStatusText}" Foreground="{StaticResource TextPrimary}" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" LineHeight="22" TextAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- Footer -->
                    <Grid Grid.Row="2">
                        <Rectangle Height="1" Fill="{StaticResource BorderColor}" VerticalAlignment="Top" Opacity="0.5" Margin="0,6,0,0"/>
                        <Border HorizontalAlignment="Center" Background="{StaticResource SidebarBackground}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,18,0,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="LAST CHECKED:" FontSize="8" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding PbeLastCheck}" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <!-- CARD 2 (Mid-Left): SYSTEM HEALTH -->
            <Border Background="{StaticResource CardBackground}" CornerRadius="12" Margin="6,0,6,0" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Padding="16,15" MinHeight="220">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                        <materialIcons:MaterialIcon Kind="Pulse" Width="18" Height="18" Foreground="{StaticResource AccentPurple}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="SYSTEM HEALTH" FontWeight="Bold" FontSize="12" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Main Content -->
                    <Grid Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Core Version -->
                        <Border Grid.Row="0" Grid.Column="0" Background="{StaticResource SidebarBackground}" CornerRadius="6" Width="28" Height="28" Margin="0,0,10,12">
                            <materialIcons:MaterialIcon Kind="{Binding AppVersionIconKind}" Width="16" Foreground="{Binding AppVersionColor}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,12">
                            <TextBlock Text="Core Version" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMuted}"/>
                            <TextBlock Text="{Binding AppVersionText}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                        </StackPanel>

                        <!-- Hash Database -->
                        <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource SidebarBackground}" CornerRadius="6" Width="28" Height="28" Margin="0,0,10,4">
                            <materialIcons:MaterialIcon Kind="DatabaseCheckOutline" Width="16" Foreground="{StaticResource AccentGreen}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,4">
                            <TextBlock Text="Hash Database" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,2"/>
                            <TextBlock Text="{Binding HashesStatus}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                        </StackPanel>
                    </Grid>

                    <!-- Footer -->
                    <Grid Grid.Row="2">
                        <Rectangle Height="1" Fill="{StaticResource BorderColor}" VerticalAlignment="Top" Opacity="0.5" Margin="0,6,0,0"/>
                        <Border HorizontalAlignment="Center" Background="#1A00E676" CornerRadius="4" Padding="6,2" Margin="0,18,0,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <materialIcons:MaterialIcon Kind="{Binding SystemHealthFooterIconKind}" Foreground="{Binding SystemHealthFooterColor}" Width="10" Height="10" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding SystemHealthFooterText}" FontSize="9" FontWeight="Bold" Foreground="{Binding SystemHealthFooterColor}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <!-- CARD 3 (Mid-Right): FILE WATCHER -->
            <Border Background="{StaticResource CardBackground}" CornerRadius="12" Margin="6,0,6,0" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Padding="16,15" MinHeight="220">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                        <materialIcons:MaterialIcon Kind="EyeSettingsOutline" Width="18" Height="18" Foreground="{StaticResource AccentGreen}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="FILE WATCHER" FontWeight="Bold" FontSize="12" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Metrics HUD -->
                    <Grid Grid.Row="1" VerticalAlignment="Center" Margin="12,0,12,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Active Files -->
                        <StackPanel Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                            <TextBlock Text="ACTIVE FILES" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMuted}" TextAlignment="Center" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding MonitoredFilesCount}" FontSize="28" FontWeight="Light" Foreground="{StaticResource TextPrimary}" TextAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                            <TextBlock Text="PENDING" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMuted}" TextAlignment="Center" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding MonitoredFilesChangedCount}" FontSize="28" FontWeight="Light" Foreground="{StaticResource AccentRed}" TextAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <!-- Footer -->
                    <Grid Grid.Row="2">
                        <Rectangle Height="1" Fill="{StaticResource BorderColor}" VerticalAlignment="Top" Opacity="0.5" Margin="0,6,0,0"/>
                        <Border HorizontalAlignment="Center" Background="{StaticResource SidebarBackground}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,18,0,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="ACTIVITY:" FontSize="8" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding WatcherLastUpdate}" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" TextAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <!-- CARD 4 (Right): ASSET TRACKER -->
            <Border Background="{StaticResource CardBackground}" CornerRadius="12" Margin="6,0,0,0" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Padding="16,15" MinHeight="220">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                        <materialIcons:MaterialIcon Kind="ImageSearchOutline" Width="18" Height="18" Foreground="{StaticResource AccentOrange}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="ASSET TRACKER" FontWeight="Bold" FontSize="12" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Metrics HUD -->
                    <Grid Grid.Row="1" VerticalAlignment="Center" Margin="12,0,12,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Library Size -->
                        <StackPanel Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                            <TextBlock Text="LIBRARY SIZE" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMuted}" TextAlignment="Center" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding AssetTrackerTotalFound}" FontSize="28" FontWeight="Light" Foreground="{StaticResource AccentGreen}" TextAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                            <TextBlock Text="CATEGORIES" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMuted}" TextAlignment="Center" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding AssetTrackerCategoriesCount}" FontSize="28" FontWeight="Light" Foreground="{StaticResource TextPrimary}" TextAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <!-- Footer -->
                    <Grid Grid.Row="2">
                        <Rectangle Height="1" Fill="{StaticResource BorderColor}" VerticalAlignment="Top" Opacity="0.5" Margin="0,6,0,0"/>
                        <Border HorizontalAlignment="Center" Background="{StaticResource SidebarBackground}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,18,0,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="STATUS:" FontSize="8" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding AssetTrackerStatus}" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" TextAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

        </UniformGrid>
    </Grid>
</UserControl>