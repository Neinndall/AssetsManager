<UserControl x:Class="AssetsManager.Views.Controls.Explorer.DetailsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             mc:Ignorable="d">
             
    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Status Card -->
            <RowDefinition Height="*"/>    <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,24">
            <Border Background="{DynamicResource HoverColor}" CornerRadius="8" Width="40" Height="40" Margin="0,0,16,0">
                <materialIcons:MaterialIcon Kind="FileEditOutline" Width="20" Height="20" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="File Details" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="Metadata information for renamed items" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
            </StackPanel>
        </StackPanel>

        <!-- Status Badge Card -->
        <Border Grid.Row="1" Background="{DynamicResource SidebarBackground}" CornerRadius="8" Padding="16" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Margin="0,0,0,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Width="40" Height="40" CornerRadius="20" Background="{DynamicResource HoverColor}" Margin="0,0,16,0">
                    <materialIcons:MaterialIcon Kind="CompareHorizontal" Width="20" Height="20" Foreground="{DynamicResource AccentOrange}"/>
                </Border>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="CHANGE TYPE" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextMuted}"/>
                    <TextBlock Text="RENAMED" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,-2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content Area -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="PATH HISTORY" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,12"/>

            <!-- Old Path -->
            <StackPanel Margin="0,0,0,16">
                <TextBlock Text="Original Path" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,6"/>
                <Border Background="{DynamicResource SidebarBackground}" CornerRadius="6" Padding="12,10" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                    <TextBlock Text="{Binding OldPath}" Foreground="{DynamicResource TextPrimary}" FontSize="13" TextWrapping="Wrap"/>
                </Border>
            </StackPanel>

            <!-- New Path -->
            <StackPanel>
                <TextBlock Text="New Path" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,6"/>
                <Border Background="{DynamicResource SidebarBackground}" CornerRadius="6" Padding="12,10" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                    <TextBlock Text="{Binding FullPath}" Foreground="{DynamicResource TextPrimary}" FontSize="13" FontWeight="SemiBold" TextWrapping="Wrap"/>
                </Border>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>