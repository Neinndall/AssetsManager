<UserControl x:Class="AssetsManager.Views.Controls.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:info="clr-namespace:AssetsManager.Info"
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="730" d:DesignWidth="180"
             Width="64">

    <Border Background="{StaticResource CardBackground}" 
            BorderBrush="{StaticResource BorderColor}" 
            BorderThickness="0,0,1,0" 
            ClipToBounds="True">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" 
                        VerticalAlignment="Top" 
                        Margin="8,16,8,0">

                <RadioButton x:Name="ToggleExpansionButton" Style="{StaticResource SidebarRadioButtonStyle}" Click="ToggleExpansion_Click" Margin="0,0,0,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialIcons:MaterialIcon Grid.Column="0" Kind="Menu" Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="12,0,0,0"/>
                        <TextBlock Grid.Column="1" Text="Panel" FontWeight="Normal" FontSize="12" VerticalAlignment="Center" Margin="16,0,12,0" Style="{StaticResource SidebarMenuTextStyle}"/>
                    </Grid>
                </RadioButton>

                <RadioButton x:Name="JsonDiffToolButton" Style="{StaticResource SidebarRadioButtonStyle}" Click="MenuButton_Click" Tag="Comparator" Margin="0,0,0,8" IsChecked="True" GroupName="SidebarNavigation">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialIcons:MaterialIcon Grid.Column="0" Kind="FileCompare" Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="12,0,0,0"/>
                        <TextBlock Grid.Column="1" Text="Comparator" FontWeight="Normal" FontSize="12" VerticalAlignment="Center" Margin="16,0,12,0" Style="{StaticResource SidebarMenuTextStyle}"/>
                    </Grid>
                </RadioButton>

                <RadioButton x:Name="ExplorerButton" Style="{StaticResource SidebarRadioButtonStyle}" Click="MenuButton_Click" Tag="Explorer" Margin="0,0,0,8" GroupName="SidebarNavigation">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialIcons:MaterialIcon Grid.Column="0" Kind="FolderSearchOutline" Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="12,0,0,0"/>
                        <TextBlock Grid.Column="1" Text="Explorer" FontWeight="Normal" FontSize="12" VerticalAlignment="Center" Margin="16,0,12,0" Style="{StaticResource SidebarMenuTextStyle}"/>
                    </Grid>
                </RadioButton>

                <RadioButton x:Name="ModelsButton" Style="{StaticResource SidebarRadioButtonStyle}" Click="MenuButton_Click" Tag="Models" Margin="0,0,0,8" GroupName="SidebarNavigation">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialIcons:MaterialIcon Grid.Column="0" Kind="CubeOutline" Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="12,0,0,0"/>
                        <TextBlock Grid.Column="1" Text="Models" FontWeight="Normal" FontSize="12" VerticalAlignment="Center" Margin="16,0,12,0" Style="{StaticResource SidebarMenuTextStyle}"/>
                    </Grid>
                </RadioButton>

                <RadioButton x:Name="MonitorButton" Style="{StaticResource SidebarRadioButtonStyle}" Click="MenuButton_Click" Tag="Monitor" Margin="0,0,0,8" GroupName="SidebarNavigation">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialIcons:MaterialIcon Grid.Column="0" Kind="Radar" Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="12,0,0,0"/>
                        <TextBlock Grid.Column="1" Text="Monitor" FontWeight="Normal" FontSize="12" VerticalAlignment="Center" Margin="16,0,12,0" Style="{StaticResource SidebarMenuTextStyle}"/>
                    </Grid>
                </RadioButton>

                <RadioButton x:Name="SettingsButton" Style="{StaticResource SidebarRadioButtonStyle}" Click="MenuButton_Click" Tag="Settings" Margin="0,0,0,8" GroupName="SidebarNavigation">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialIcons:MaterialIcon Grid.Column="0" Kind="Cog" Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="12,0,0,0"/>
                        <TextBlock Grid.Column="1" Text="Settings" FontWeight="Normal" FontSize="12" VerticalAlignment="Center" Margin="16,0,12,0" Style="{StaticResource SidebarMenuTextStyle}"/>
                    </Grid>
                </RadioButton>

                <RadioButton x:Name="HelpButton" Style="{StaticResource SidebarRadioButtonStyle}" Click="MenuButton_Click" Tag="Help" Margin="0,0,0,8" GroupName="SidebarNavigation">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialIcons:MaterialIcon Grid.Column="0" Kind="HelpCircleOutline" Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="12,0,0,0"/>
                        <TextBlock Grid.Column="1" Text="Help" FontWeight="Normal" FontSize="12" VerticalAlignment="Center" Margin="16,0,12,0" Style="{StaticResource SidebarMenuTextStyle}"/>
                    </Grid>
                </RadioButton>
            </StackPanel>

            <Border Grid.Row="1" Margin="8,0,8,8">
                <TextBlock Text="{x:Static info:ApplicationInfos.Version}" FontWeight="Normal" FontSize="10" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center" Style="{StaticResource SidebarMenuTextStyle}"/>
            </Border>
        </Grid>
    </Border>
</UserControl>