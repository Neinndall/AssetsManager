<UserControl x:Class="AssetsManager.Views.Settings.AdvancedSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="850">

    <UserControl.Resources>
        <x:Array x:Key="UpdateFrequencies" Type="{x:Type System:Int32}">
            <System:Int32>1</System:Int32>
            <System:Int32>10</System:Int32>
            <System:Int32>30</System:Int32>
            <System:Int32>60</System:Int32>
        </x:Array>
    </UserControl.Resources>

    <StackPanel Margin="0,16,0,40">
        <!-- SECTION: ASSET DISCOVERY -->
        <StackPanel Margin="0,0,0,40">
            <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Padding="0,0,0,12" Margin="0,0,0,24">
                <StackPanel Orientation="Horizontal">
                    <materialIcons:MaterialIcon Kind="Radar" Foreground="{StaticResource AccentBrush}" Width="18" Height="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="ASSET DISCOVERY" FontSize="12" FontWeight="Black" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="0,1,0,0"/>
                </StackPanel>
            </Border>

            <StackPanel Margin="28,0,0,0">
                <!-- Asset Tracker (Last item of section, removed Margin-Bottom) -->
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Automated CDN tracking" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" FontSize="13"/>
                            <TextBlock Text="Periodically scan Riot's CDN for newly discovered assets" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,2,0,0"/>
                        </StackPanel>
                        <CheckBox x:Name="swAssetTracker" Grid.Column="1" IsChecked="{Binding Settings.AssetTrackerTimer, Mode=TwoWay}" Style="{StaticResource ModernToggleSwitch}"/>
                    </Grid>
                    
                    <Border Margin="0,12,0,0" Background="{StaticResource HoverColor}" CornerRadius="4" Padding="16,10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1"
                            Visibility="{Binding IsChecked, ElementName=swAssetTracker, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="SCAN FREQUENCY (MINUTES)" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}" FontSize="10" FontWeight="Black"/>
                            <ComboBox Grid.Column="1" SelectedItem="{Binding Settings.AssetTrackerFrequency, Mode=TwoWay}" Width="80" Height="26" Style="{StaticResource ModernComboBox}" ItemsSource="{StaticResource UpdateFrequencies}"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <!-- SECTION: PBE STATUS -->
        <StackPanel Margin="0,0,0,40">
            <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Padding="0,0,0,12" Margin="0,0,0,24">
                <StackPanel Orientation="Horizontal">
                    <materialIcons:MaterialIcon Kind="LanPending" Foreground="{StaticResource AccentBrush}" Width="18" Height="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="PBE STATUS" FontSize="12" FontWeight="Black" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="0,1,0,0"/>
                </StackPanel>
            </Border>

            <StackPanel Margin="28,0,0,0">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Get server maintenance for PBE updates" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" FontSize="13"/>
                            <TextBlock Text="Real-time alerts for maintenance and server downtime" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,2,0,0"/>
                        </StackPanel>
                        <CheckBox x:Name="swPbeStatus" Grid.Column="1" IsChecked="{Binding Settings.CheckPbeStatus, Mode=TwoWay}" Style="{StaticResource ModernToggleSwitch}"/>
                    </Grid>

                    <Border Margin="0,12,0,0" Background="{StaticResource HoverColor}" CornerRadius="4" Padding="16,10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1"
                            Visibility="{Binding IsChecked, ElementName=swPbeStatus, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="CHECK INTERVAL (MINUTES)" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}" FontSize="10" FontWeight="Black"/>
                            <ComboBox Grid.Column="1" SelectedValue="{Binding Settings.PbeStatusFrequency, Mode=TwoWay}" Width="80" Height="28" Style="{StaticResource ModernComboBox}" ItemsSource="{StaticResource UpdateFrequencies}"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <!-- SECTION: API INTEGRATION -->
        <StackPanel Margin="0,0,0,40">
            <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Padding="0,0,0,12" Margin="0,0,0,24">
                <StackPanel Orientation="Horizontal">
                    <materialIcons:MaterialIcon Kind="Api" Foreground="{StaticResource AccentBrush}" Width="18" Height="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="API INTEGRATION" FontSize="12" FontWeight="Black" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="0,1,0,0"/>
                </StackPanel>
            </Border>

            <StackPanel Margin="28,0,0,0">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Prioritize PBE for API Authentication" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" FontSize="13"/>
                        <TextBlock Text="Forces LCU API to use PBE credentials over Live client" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                    <CheckBox Grid.Column="1" IsChecked="{Binding Settings.ApiSettings.UsePbeForApi, Mode=TwoWay}" Style="{StaticResource ModernToggleSwitch}"/>
                </Grid>
            </StackPanel>
        </StackPanel>

        <!-- SECTION: SYSTEM ARCHITECTURE -->
        <StackPanel>
            <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Padding="0,0,0,12" Margin="0,0,0,24">
                <StackPanel Orientation="Horizontal">
                    <materialIcons:MaterialIcon Kind="ApplicationCogOutline" Foreground="{StaticResource AccentBrush}" Width="18" Height="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="SYSTEM ARCHITECTURE" FontSize="12" FontWeight="Black" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="0,1,0,0"/>
                </StackPanel>
            </Border>

            <StackPanel Margin="28,0,0,0">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Run in background on close" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" FontSize="13"/>
                        <TextBlock Text="Minimize to system tray on close to maintain monitoring" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                    <CheckBox Grid.Column="1" IsChecked="{Binding Settings.MinimizeToTrayOnClose, Mode=TwoWay}" Style="{StaticResource ModernToggleSwitch}"/>
                </Grid>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>
