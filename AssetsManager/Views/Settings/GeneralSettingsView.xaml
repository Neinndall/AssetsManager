<UserControl x:Class="AssetsManager.Views.Settings.GeneralSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="800">

    <UserControl.Resources>
        <x:Array x:Key="UpdateFrequencies" Type="{x:Type System:Int32}">
            <System:Int32>1</System:Int32>
            <System:Int32>10</System:Int32>
            <System:Int32>30</System:Int32>
            <System:Int32>60</System:Int32>
        </x:Array>
    </UserControl.Resources>

    <StackPanel Margin="0,0,0,40">
        <!-- MODERN INTEGRATED HEADER (Compact) -->
        <Border Margin="0,0,0,20" Background="{StaticResource CardBackground}" CornerRadius="12" Padding="20,14" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{StaticResource HoverColor}" CornerRadius="10" Width="44" Height="44" Margin="0,0,16,0">
                    <materialIcons:MaterialIcon Kind="CogOutline" Width="22" Height="22" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="General Settings" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="Configure basic application behavior and synchronization options" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,1,0,0"/>
                </StackPanel>

                <!-- Subtle Decorative Background Icon -->
                <materialIcons:MaterialIcon Grid.Column="1" Kind="Database" Width="64" Height="64" Foreground="{StaticResource AccentBrush}" Opacity="0.05" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,-8,0"/>
            </Grid>
        </Border>

        <!-- SECTION 1: Data & History -->
        <Border Style="{StaticResource SettingsCard}">
            <StackPanel>
                <!-- Header -->
                <Grid Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Background="{StaticResource HoverColor}" CornerRadius="10" Width="40" Height="40" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                        <materialIcons:MaterialIcon Kind="DatabaseOutline" Foreground="{StaticResource AccentPurple}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0">
                        <TextBlock Text="Data and History" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Manage data synchronization and saving differences" FontSize="12" Foreground="{StaticResource TextMuted}" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
                
                <!-- Content -->
                <Grid Margin="8,0,0,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="2" Background="{StaticResource AccentPurple}" Opacity="0.2" CornerRadius="1" Margin="0,4,20,4"/>
                    
                    <StackPanel Grid.Column="1">
                        <CheckBox IsChecked="{Binding Settings.SyncHashesWithCDTB, Mode=TwoWay}" Content="Synchronization with CDTB" Style="{StaticResource ModernCheckBox}"/>
                        <TextBlock Text="Automatically sync with the latest hash database from CDTB servers" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="28,4,0,16" TextWrapping="Wrap"/>

                        <CheckBox IsChecked="{Binding Settings.SaveDiffHistory, Mode=TwoWay}" Content="Save differences history" Style="{StaticResource ModernCheckBox}"/>
                        <TextBlock Text="Save a history of all JSON file differences detected during updates" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="28,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- SECTION 2: Automatic Checks -->
        <Border Style="{StaticResource SettingsCard}">
            <StackPanel>
                <!-- Header -->
                <Grid Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="{StaticResource HoverColor}" CornerRadius="10" Width="40" Height="40" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                        <materialIcons:MaterialIcon Kind="Sync" Foreground="{StaticResource AccentBrush}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0">
                        <TextBlock Text="Automatic Checks" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Configure what to look for and how often" FontSize="12" Foreground="{StaticResource TextMuted}" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- Content -->
                <Grid Margin="8,0,0,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="2" Background="{StaticResource AccentBrush}" Opacity="0.2" CornerRadius="1" Margin="0,4,20,4"/>

                    <StackPanel Grid.Column="1">
                        <CheckBox x:Name="checkBoxBackgroundUpdates" IsChecked="{Binding Settings.BackgroundUpdates, Mode=TwoWay}" Content="Background updates" Style="{StaticResource ModernCheckBox}"/>
                        <TextBlock Text="Periodically check for updates while the application is running" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="28,4,0,16" TextWrapping="Wrap"/>

                        <!-- Frequency Selector -->
                        <Border Margin="28,0,0,16" Background="{StaticResource SidebarBackground}" CornerRadius="8" Padding="16,12" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=checkBoxBackgroundUpdates}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Update Interval" Foreground="{StaticResource TextSecondary}" FontSize="13" FontWeight="SemiBold"/>
                                    <TextBlock Text="Time in minutes between checks" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                </StackPanel>
                                <ComboBox Grid.Column="1" SelectedItem="{Binding Settings.UpdateCheckFrequency, Mode=TwoWay}" Width="100" Height="32" Style="{StaticResource ModernComboBox}" ItemsSource="{StaticResource UpdateFrequencies}"/>
                            </Grid>
                        </Border>

                        <CheckBox IsChecked="{Binding Settings.CheckJsonDataUpdates, Mode=TwoWay}" Content="Check for files updates" Style="{StaticResource ModernCheckBox}"/>
                        <TextBlock Text="Check for updates to json files and their differences" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="28,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- SECTION 3: File Management -->
        <Border Style="{StaticResource SettingsCard}">
            <StackPanel>
                <!-- Header -->
                <Grid Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="{StaticResource HoverColor}" CornerRadius="10" Width="40" Height="40" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                        <materialIcons:MaterialIcon Kind="FolderSyncOutline" Foreground="{StaticResource AccentGreen}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0">
                        <TextBlock Text="File Management" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Manage asset extraction and organization" FontSize="12" Foreground="{StaticResource TextMuted}" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- Content -->
                <Grid Margin="8,0,0,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="2" Background="{StaticResource AccentGreen}" Opacity="0.2" CornerRadius="1" Margin="0,4,20,4"/>

                    <StackPanel Grid.Column="1">
                        <CheckBox IsChecked="{Binding Settings.EnableExtraction, Mode=TwoWay}" 
                                  x:Name="EnableExtractionCheckBox" 
                                  Content="Auto-extract new files"
                                  Style="{StaticResource ModernCheckBox}"
                                  IsEnabled="{Binding IsChecked, ElementName=ReportGenerationCheckBox, Converter={StaticResource InverseBooleanConverter}}"/>
                        <TextBlock Text="Extract found files after comparison to AssetsDownloaded folder" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="28,4,0,16" TextWrapping="Wrap"/>

                        <CheckBox IsChecked="{Binding Settings.OrganizeExtractedAssets, Mode=TwoWay}" 
                                  Content="Organize extracted files into folders"
                                  Style="{StaticResource ModernCheckBox}"
                                  IsEnabled="{Binding IsChecked, ElementName=EnableExtractionCheckBox}"/>
                        <TextBlock Text="If enabled, extracted assets will be organized into folders" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="28,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- SECTION 4: Export Settings -->
        <Border Style="{StaticResource SettingsCard}">
            <StackPanel>
                <!-- Header -->
                <Grid Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="{StaticResource HoverColor}" CornerRadius="10" Width="40" Height="40" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                        <materialIcons:MaterialIcon Kind="FileExportOutline" Foreground="{StaticResource AccentBlue}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0">
                        <TextBlock Text="Export Settings" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Configure default formats for asset extraction" FontSize="12" Foreground="{StaticResource TextMuted}" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- Content -->
                <Grid Margin="8,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0" Width="2" Background="{StaticResource AccentBlue}" Opacity="0.2" CornerRadius="1" Margin="0,4,20,4"/>

                    <StackPanel Grid.Column="1">
                        <!-- Audio Format -->
                        <Grid Margin="0,0,0,24">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="Preferred Audio Format" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" />
                                <TextBlock Text="Format to use when exporting .wem files" FontSize="12" Margin="0,4,0,0" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" />
                            </StackPanel>

                            <ComboBox Grid.Column="1"
                                      Width="140"
                                      Height="32"
                                      ItemsSource="{Binding AudioFormatOptions}"
                                      DisplayMemberPath="Name"
                                      SelectedValuePath="Value"
                                      SelectedValue="{Binding AudioExportFormat, Mode=TwoWay}"
                                      Style="{StaticResource ModernComboBox}"
                                      VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Info for MP3 -->
                        <Border Margin="0,-16,0,24" 
                                Padding="12" 
                                Background="{StaticResource SidebarBackground}" 
                                CornerRadius="8">
                             <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AudioExportFormat}" Value="Mp3">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                             </Border.Style>
                             <StackPanel Orientation="Horizontal">
                                <materialIcons:MaterialIcon Kind="InformationOutline" Width="16" Height="16" Foreground="{StaticResource TextSecondary}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="MP3 conversion might take slightly longer due to encoding." 
                                           FontSize="11" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,1,0,0"/>
                             </StackPanel>
                        </Border>

                        <!-- Image Format -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="Preferred Image Format" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" />
                                <TextBlock Text="Format to use when exporting .tex/.dds files" FontSize="12" Margin="0,4,0,0" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" />
                            </StackPanel>

                            <ComboBox Grid.Column="1"
                                      Width="140"
                                      Height="32"
                                      ItemsSource="{Binding ImageFormatOptions}"
                                      DisplayMemberPath="Name"
                                      SelectedValuePath="Value"
                                      SelectedValue="{Binding ImageExportFormat, Mode=TwoWay}"
                                      Style="{StaticResource ModernComboBox}"
                                      VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- SECTION 5: Report Generation -->
        <Border Style="{StaticResource SettingsCard}">
            <StackPanel>
                <!-- Header -->
                <Grid Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="{StaticResource HoverColor}" CornerRadius="10" Width="40" Height="40" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                        <materialIcons:MaterialIcon Kind="FileDocumentOutline" Foreground="{StaticResource AccentOrange}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0">
                        <TextBlock Text="Report Generation" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Generate a log file with asset changes after a comparison" FontSize="12" Foreground="{StaticResource TextMuted}" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- Content -->
                <Grid Margin="8,0,0,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="2" Background="{StaticResource AccentOrange}" Opacity="0.2" CornerRadius="1" Margin="0,4,20,4"/>

                    <StackPanel Grid.Column="1">
                        <CheckBox x:Name="ReportGenerationCheckBox" 
                                  IsChecked="{Binding Settings.ReportGeneration.Enabled, Mode=TwoWay}" 
                                  Content="Generate asset report"
                                  Style="{StaticResource ModernCheckBox}"
                                  IsEnabled="{Binding IsChecked, ElementName=EnableExtractionCheckBox, Converter={StaticResource InverseBooleanConverter}}"/>
                        <TextBlock Text="An assets.log file will be created instead of extracting the assets" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="28,4,0,16" TextWrapping="Wrap"/>

                        <!-- Filter Options -->
                        <Border Margin="28,0,0,8" Background="{StaticResource SidebarBackground}" CornerRadius="8" Padding="16,12" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=ReportGenerationCheckBox}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            
                            <StackPanel>
                                <TextBlock Text="Include in report:" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,12"/>
                                <WrapPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding Settings.ReportGeneration.FilterNew, Mode=TwoWay}" Content="New" Style="{StaticResource ModernCheckBox}" Margin="0,0,16,8"/>
                                    <CheckBox IsChecked="{Binding Settings.ReportGeneration.FilterModified, Mode=TwoWay}" Content="Modified" Style="{StaticResource ModernCheckBox}" Margin="0,0,16,8"/>
                                    <CheckBox IsChecked="{Binding Settings.ReportGeneration.FilterRenamed, Mode=TwoWay}" Content="Renamed" Style="{StaticResource ModernCheckBox}" Margin="0,0,16,8"/>
                                    <CheckBox IsChecked="{Binding Settings.ReportGeneration.FilterRemoved, Mode=TwoWay}" Content="Removed" Style="{StaticResource ModernCheckBox}" Margin="0,0,16,8"/>
                                </WrapPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
