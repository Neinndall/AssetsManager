<UserControl x:Class="AssetsManager.Views.Help.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <!-- Page Header -->
        <StackPanel Margin="0,0,0,32">
            <TextBlock Text="About"
                       FontSize="28"
                       FontWeight="Light"
                       Foreground="{StaticResource TextPrimary}"
                       Margin="0,4,0,0"/>
            <TextBlock Text="Information about the application"
                       FontSize="14"
                       Foreground="{StaticResource TextMuted}"
                       Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Description Card -->
        <Border Style="{StaticResource SettingsCard}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <materialDesign:MaterialIcon Kind="Information" 
                                               Foreground="{StaticResource AccentBrush}"
                                               FontSize="20" 
                                               Margin="0,0,12,0" 
                                               VerticalAlignment="Center"/>
                    <StackPanel>
                        <TextBlock Text="Description"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="General information about the application" 
                                     FontSize="12" 
                                     Foreground="{StaticResource TextMuted}"/>
                    </StackPanel>
                </StackPanel>
                <TextBlock Text="AssetsManager is a tool for League of Legends enthusiasts who need to analyze, manage, and track changes to game assets from PBE server updates. It goes beyond simple downloading, offering a powerful suite of features for deep asset analysis, a 3D model viewer, exploration, and monitoring for changes."
                           Foreground="{StaticResource TextPrimary}"
                           FontSize="13"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Key Features Card -->
        <Border Style="{StaticResource SettingsCard}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <materialDesign:MaterialIcon Kind="Star"
                                               Foreground="Gold"
                                               FontSize="20" 
                                               Margin="0,0,12,0" 
                                               VerticalAlignment="Center"/>
                    <StackPanel>
                        <TextBlock Text="Key Features"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Discover the main functionalities of the application" 
                                     FontSize="12" 
                                     Foreground="{StaticResource TextMuted}"/>
                    </StackPanel>
                </StackPanel>

                <TextBlock Margin="0,4" FontSize="13" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Text="•  Automatic Asset Downloading: Automatically detects and downloads new assets from PBE updates."/>
                <TextBlock Margin="0,4" FontSize="13" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Text="•  Advanced WAD Comparator: Compares WAD files between different versions to identify new, modified, or deleted assets."/>
                <TextBlock Margin="0,4" FontSize="13" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Text="•  Powerful File Explorer: Explores WAD archives with a file tree interface and previews dozens of formats."/>
                <TextBlock Margin="0,4" FontSize="13" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Text="•  3D Model Viewer: Inspects 3D models (.skn), skeletons (.skl), and animations (.anm) with a built-in viewer."/>
                <TextBlock Margin="0,4" FontSize="13" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Text="•  Asset Monitoring: Tracks remote assets, JSON files, and the PBE server status for real-time updates."/>
                <TextBlock Margin="0,4" FontSize="13" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Text="•  Version Management: Easily manages and downloads different versions of the League of Legends client and its plugins."/>
                <TextBlock Margin="0,4" FontSize="13" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Text="•  Smart Extraction and Saving: Extracts raw assets or saves them in more user-friendly formats (e.g., .tex to .png, .bin to .json)."/>
                <TextBlock Margin="0,4" FontSize="13" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Text="•  Audio Bank Analysis: Explores audio banks (.wpk, .bnk), visualizes the event hierarchy with their names, and plays the associated sounds."/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>