<UserControl x:Class="AssetsManager.Views.Help.ChangelogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <StackPanel>
        <!-- MODERN INTEGRATED HEADER (Compact) -->
        <Border Margin="0,0,0,20" Background="{StaticResource CardBackground}" CornerRadius="12" Padding="20,14" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{StaticResource HoverColor}" CornerRadius="10" Width="44" Height="44" Margin="0,0,16,0">
                    <materialIcons:MaterialIcon Kind="History" Width="22" Height="22" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Changelogs" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="Version history and feature updates" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,1,0,0"/>
                </StackPanel>

                <!-- Subtle Decorative Background Icon -->
                <materialIcons:MaterialIcon Grid.Column="1" Kind="Tag" Width="64" Height="64" Foreground="{StaticResource AccentBrush}" Opacity="0.05" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,-8,0"/>
            </Grid>
        </Border>

        <ItemsControl x:Name="ChangelogItemsControl">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <!-- VERSION CARD -->
                    <Border Style="{StaticResource SettingsCard}" Margin="0,0,0,24">
                        <StackPanel>
                            <!-- Version Badge Header -->
                            <Grid Margin="0,0,0,20">
                                <Border Background="{StaticResource HoverColor}" 
                                        CornerRadius="6" 
                                        HorizontalAlignment="Left" 
                                        Padding="12,6"
                                        BorderBrush="{StaticResource BorderColor}"
                                        BorderThickness="1">
                                    <StackPanel Orientation="Horizontal">
                                        <materialIcons:MaterialIcon Kind="Tag" Foreground="{StaticResource AccentBrush}" Width="14" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding Version}" 
                                                   FontWeight="Bold" 
                                                   FontSize="14" 
                                                   Foreground="{StaticResource TextPrimary}"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- GROUPS (New, Fixes, etc) -->
                            <ItemsControl ItemsSource="{Binding Groups}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,16">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Group Icon Column -->
                                            <Border Width="32" Height="32" CornerRadius="16" Background="{StaticResource HoverColor}" VerticalAlignment="Top">
                                                <materialIcons:MaterialIcon Kind="{Binding Icon}" 
                                                                            Foreground="{Binding IconColor}"
                                                                            Width="16" Height="16"
                                                                            HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center"/>
                                            </Border>
                                            
                                            <!-- Group Content -->
                                            <!-- Reduced margin from 16 to 12 to bring content closer to icon -->
                                            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                                <!-- Group Title -->
                                                <TextBlock Text="{Binding Title}"
                                                           FontSize="15"
                                                           FontWeight="SemiBold"
                                                           Foreground="{StaticResource TextPrimary}"
                                                           Margin="0,4,0,8"/>

                                                <!-- CHANGES LIST -->
                                                <ItemsControl ItemsSource="{Binding Changes}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Margin="0,3">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto"/> <!-- Indentation Spacer -->
                                                                    <ColumnDefinition Width="Auto"/> <!-- Bullet -->
                                                                    <ColumnDefinition Width="*"/>    <!-- Text -->
                                                                </Grid.ColumnDefinitions>

                                                                <!-- Indentation Column -->
                                                                <Border Grid.Column="0">
                                                                    <Border.Style>
                                                                        <Style TargetType="Border">
                                                                            <Setter Property="Width" Value="0"/>
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IndentationLevel}" Value="2">
                                                                                    <Setter Property="Width" Value="12"/>
                                                                                </DataTrigger>
                                                                                <DataTrigger Binding="{Binding IndentationLevel}" Value="3">
                                                                                    <Setter Property="Width" Value="24"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </Border.Style>
                                                                </Border>

                                                                <!-- Bullet Point: Fixed alignment and visibility -->
                                                                <Ellipse Grid.Column="1" Width="4" Height="4" 
                                                                         Fill="{StaticResource TextMuted}" 
                                                                         VerticalAlignment="Top" 
                                                                         Margin="0,7,8,0">
                                                                    <Ellipse.Style>
                                                                        <Style TargetType="Ellipse">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IsSubheading}" Value="True">
                                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                                </DataTrigger>
                                                                                <DataTrigger Binding="{Binding IsDescription}" Value="True">
                                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </Ellipse.Style>
                                                                </Ellipse>

                                                                <!-- TEXT CONTENT -->
                                                                <TextBlock Grid.Column="2" TextWrapping="Wrap">
                                                                    <TextBlock.Style>
                                                                        <Style TargetType="TextBlock">
                                                                            <Setter Property="Text" Value="{Binding Text}"/>
                                                                            <Setter Property="FontSize" Value="13"/>
                                                                            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                                                            
                                                                            <Style.Triggers>
                                                                                <!-- Subheading Style: Bold and closer to left -->
                                                                                <DataTrigger Binding="{Binding IsSubheading}" Value="True">
                                                                                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                                                    <Setter Property="Margin" Value="0,4,0,2"/>
                                                                                </DataTrigger>
                                                                                
                                                                                <!-- Description Style -->
                                                                                <DataTrigger Binding="{Binding IsDescription}" Value="True">
                                                                                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                                                    <Setter Property="FontStyle" Value="Italic"/>
                                                                                    <Setter Property="FontSize" Value="12"/>
                                                                                    <Setter Property="Margin" Value="0,0,0,2"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </TextBlock.Style>
                                                                </TextBlock>
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>