<Window x:Class="AssetsManager.Views.Dialogs.ImageMergerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:icons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
        mc:Ignorable="d" FontFamily="{StaticResource AppFontFamily}"
        Title="Image Merger" Height="700" Width="1100"
        MinHeight="700" MinWidth="1100"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="True"
        Background="Transparent"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" 
                      ResizeBorderThickness="5" 
                      GlassFrameThickness="0" 
                      CornerRadius="0" 
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Border BorderBrush="{StaticResource BorderColor}" 
            Background="{StaticResource CardBackground}"
            BorderThickness="1" 
            CornerRadius="12" 
            Margin="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- 0: Custom Title Bar -->
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 1. Custom Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource CardBackground}" Height="40" CornerRadius="11,11,0,0" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Panel.ZIndex="10">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="1" Color="Black" Opacity="0.15" Direction="270"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/> <!-- Icon/Title -->
                        <ColumnDefinition Width="*"/>    <!-- Spacer (Drag Area) -->
                        <ColumnDefinition Width="Auto"/> <!-- Window Controls -->
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                        <icons:MaterialIcon Kind="ViewGridPlus" Width="18" Height="18" Foreground="{StaticResource AccentBrush}" Margin="0,0,10,0"/>
                        <TextBlock Text="Image Merger" 
                                   FontSize="12" 
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimary}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Style="{StaticResource TitleBarButtonStyle}" Click="Minimize_Click" Width="34" Height="40">
                            <icons:MaterialIcon Kind="WindowMinimize" Width="15" Height="15"/>
                        </Button>
                        <Button Style="{StaticResource TitleBarButtonStyle}" Click="Close_Click" Width="34" Height="40" Margin="0,0,2,0">
                            <icons:MaterialIcon Kind="WindowClose" Width="15" Height="15"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel: Management -->
                <Border Grid.Column="0" Background="{StaticResource SidebarBackground}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,0" CornerRadius="0,0,0,11">
                    <Grid Margin="15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,15">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <icons:MaterialIcon Kind="FormatListBulleted" Width="14" Height="14" Foreground="{StaticResource AccentBrush}" Opacity="0.7" VerticalAlignment="Center"/>
                                <TextBlock Text="IMAGE LIST" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </StackPanel>
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0" Style="{StaticResource PrimaryButton}" Click="AddImage_Click" Padding="10,5" Margin="0,0,4,0">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <icons:MaterialIcon Kind="Plus" Width="16" Height="16" VerticalAlignment="Center"/>
                                        <TextBlock Text="Add" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Click="ClearAll_Click" Padding="10,5" Margin="4,0,0,0" Content="Clear All" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                            </Grid>
                        </StackPanel>

                        <!-- Items List -->
                        <ListBox Grid.Row="1" x:Name="ImagesListBox" 
                                 ItemsSource="{Binding Items}" 
                                 Background="Transparent" BorderThickness="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ItemContainerStyle="{StaticResource ModernSelectableCardItemStyle}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Image Grid.Column="0" Source="{Binding Image}" Width="32" Height="32" Stretch="Uniform" VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                                            <TextBlock Text="{Binding Name}" 
                                                       FontWeight="SemiBold" FontSize="12"
                                                       Foreground="{StaticResource TextPrimary}"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="Image Asset" 
                                                       FontSize="10" 
                                                       Foreground="{StaticResource TextSecondary}"/>
                                        </StackPanel>
                                        
                                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Button Style="{StaticResource TransparentIconButton}" Click="MoveUp_Click" Tag="{Binding}" ToolTip="Move Up">
                                                <icons:MaterialIcon Kind="ArrowUp" Width="14" Height="14"/>
                                            </Button>
                                            <Button Style="{StaticResource TransparentIconButton}" Click="MoveDown_Click" Tag="{Binding}" ToolTip="Move Down">
                                                <icons:MaterialIcon Kind="ArrowDown" Width="14" Height="14"/>
                                            </Button>
                                            <Button Style="{StaticResource TransparentIconButton}" Click="Remove_Click" Tag="{Binding}" ToolTip="Remove">
                                                <icons:MaterialIcon Kind="TrashCanOutline" Width="14" Height="14" Foreground="{StaticResource AccentRed}"/>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Controls Footer -->
                        <StackPanel Grid.Row="2" Margin="0,15,0,0">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <icons:MaterialIcon Kind="Cog" Width="14" Height="14" Foreground="{StaticResource AccentBrush}" Opacity="0.7" VerticalAlignment="Center"/>
                                <TextBlock Text="SETTINGS" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </StackPanel>
                            
                            <TextBlock Text="Columns" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                            <Slider Value="{Binding Columns}" Minimum="1" Maximum="20" IsSnapToTickEnabled="True" TickFrequency="1" Margin="0,0,0,15"/>

                            <TextBlock Text="Margin" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                            <Slider Value="{Binding Margin}" Minimum="0" Maximum="50" IsSnapToTickEnabled="True" TickFrequency="1" Margin="0,0,0,15"/>

                            <Button Style="{StaticResource PrimaryButton}" Click="Export_Click" Height="40" Margin="0,10,0,0">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <icons:MaterialIcon Kind="ContentSave" Width="20" Height="20" VerticalAlignment="Center"/>
                                    <TextBlock Text="EXPORT MERGED IMAGE" Margin="10,0,0,0" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Right Panel: Preview -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{StaticResource CardBackground}" Padding="15,10" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <icons:MaterialIcon Kind="InformationOutline" Width="16" Height="16" Foreground="{StaticResource AccentBrush}" Opacity="0.7"/>
                                <TextBlock Text="PREVIEW" VerticalAlignment="Center" Margin="8,0" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}"/>
                            </StackPanel>
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <icons:MaterialIcon Kind="MagnifyPlusOutline" Width="18" Height="18" Foreground="{StaticResource TextSecondary}" Margin="0,0,8,0" Opacity="0.5"/>
                                <Slider Width="150" Value="{Binding Zoom}" Minimum="0.1" Maximum="2.0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Preview Area -->
                    <Border Grid.Row="1" Background="{StaticResource SidebarBackground}" CornerRadius="0,0,11,0">
                        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="Transparent">
                            <Grid Margin="40">
                                <Image Source="{Binding PreviewImage}" Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Image.LayoutTransform>
                                        <ScaleTransform ScaleX="{Binding Zoom}" ScaleY="{Binding Zoom}"/>
                                    </Image.LayoutTransform>
                                    <Image.Effect>
                                        <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.5"/>
                                    </Image.Effect>
                                </Image>
                            </Grid>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>