<Window x:Class="AssetsManager.Views.Dialogs.ImageMergerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
        mc:Ignorable="d"
        Title="Image Merger" Height="700" Width="1100"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource CardBackground}"
        Foreground="{StaticResource TextPrimary}"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/Colors.xaml"/>
                <ResourceDictionary Source="/Themes/ModernButton.xaml"/>
                <ResourceDictionary Source="/Themes/ModernProgressBar.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>

            <!-- Official ListBoxItem Style (From ModelPanelControl) -->
            <Style x:Key="MergerListBoxItemStyle" TargetType="ListBoxItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Grid x:Name="RootGrid" SnapsToDevicePixels="True">
                                <!-- 1. Background Layer -->
                                <Border x:Name="BackgroundBorder" Background="{StaticResource CardBackground}" CornerRadius="8"/>

                                <!-- 2. Content Layer -->
                                <Grid Margin="1">
                                    <Grid Margin="12,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ContentPresenter Grid.Column="0" />
                                        <materialIcons:MaterialIcon x:Name="CheckIcon" Grid.Column="1" Kind="CheckCircle" Foreground="{StaticResource AccentBrush}" Width="18" Height="18" Margin="8,0,0,0" Visibility="Hidden"/>
                                    </Grid>
                                </Grid>

                                <!-- 3. Border Overlay -->
                                <Border x:Name="SelectionBorder" 
                                        BorderBrush="{StaticResource BorderColor}" 
                                        BorderThickness="1" 
                                        CornerRadius="8" 
                                        IsHitTestVisible="False"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="SelectionBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                    <Setter TargetName="SelectionBorder" Property="BorderThickness" Value="1.5"/>
                                    <Setter TargetName="BackgroundBorder" Property="Background" Value="{StaticResource HoverColor}"/>
                                    <Setter TargetName="CheckIcon" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="BackgroundBorder" Property="Background" Value="{StaticResource HoverColor}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="12" Background="{StaticResource CardBackground}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" Background="{StaticResource SidebarBackground}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                    <materialIcons:MaterialIcon Kind="ViewGridPlus" Width="18" Height="18" Foreground="{StaticResource AccentBrush}"/>
                    <TextBlock Text="Image Merger" Margin="10,0,0,0" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="5,0">
                    <Button Style="{StaticResource TitleBarButtonStyle}" Click="Minimize_Click">
                        <materialIcons:MaterialIcon Kind="Minus" Width="18" Height="18"/>
                    </Button>
                    <Button Style="{StaticResource TitleBarButtonStyle}" Click="Close_Click">
                        <materialIcons:MaterialIcon Kind="Close" Width="18" Height="18"/>
                    </Button>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel: Management -->
                <Border Grid.Column="0" Background="{StaticResource SidebarBackground}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,0">
                    <Grid Margin="15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,15">
                            <TextBlock Text="IMAGE LIST" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,10"/>
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0" Style="{StaticResource PrimaryButton}" Click="AddImage_Click" Padding="10,5" Margin="0,0,4,0">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <materialIcons:MaterialIcon Kind="Plus" Width="16" Height="16" VerticalAlignment="Center"/>
                                        <TextBlock Text="Add" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Click="ClearAll_Click" Padding="10,5" Margin="4,0,0,0" Content="Clear All" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                            </Grid>
                        </StackPanel>

                        <!-- Items List -->
                        <ListBox Grid.Row="1" x:Name="ImagesListBox" 
                                 ItemsSource="{Binding Items}" 
                                 Background="Transparent" BorderThickness="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ItemContainerStyle="{StaticResource MergerListBoxItemStyle}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Image Grid.Column="0" Source="{Binding Image}" Width="32" Height="32" Stretch="Uniform" VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                                            <TextBlock Text="{Binding Name}" 
                                                       FontWeight="SemiBold" FontSize="12"
                                                       Foreground="{StaticResource TextPrimary}"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="Image Asset" 
                                                       FontSize="10" 
                                                       Foreground="{StaticResource TextSecondary}"/>
                                        </StackPanel>
                                        
                                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Button Style="{StaticResource TransparentIconButton}" Click="MoveUp_Click" Tag="{Binding}" ToolTip="Move Up">
                                                <materialIcons:MaterialIcon Kind="ArrowUp" Width="14" Height="14"/>
                                            </Button>
                                            <Button Style="{StaticResource TransparentIconButton}" Click="MoveDown_Click" Tag="{Binding}" ToolTip="Move Down">
                                                <materialIcons:MaterialIcon Kind="ArrowDown" Width="14" Height="14"/>
                                            </Button>
                                            <Button Style="{StaticResource TransparentIconButton}" Click="Remove_Click" Tag="{Binding}" ToolTip="Remove">
                                                <materialIcons:MaterialIcon Kind="TrashCanOutline" Width="14" Height="14" Foreground="{StaticResource AccentRed}"/>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Controls Footer -->
                        <StackPanel Grid.Row="2" Margin="0,15,0,0">
                            <TextBlock Text="SETTINGS" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,10"/>
                            
                            <TextBlock Text="Columns" Margin="0,0,0,5"/>
                            <Slider Value="{Binding Columns}" Minimum="1" Maximum="20" IsSnapToTickEnabled="True" TickFrequency="1" Margin="0,0,0,15"/>

                            <TextBlock Text="Margin" Margin="0,0,0,5"/>
                            <Slider Value="{Binding Margin}" Minimum="0" Maximum="50" IsSnapToTickEnabled="True" TickFrequency="1" Margin="0,0,0,15"/>

                            <Button Style="{StaticResource PrimaryButton}" Click="Export_Click" Height="40" Margin="0,10,0,0">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <materialIcons:MaterialIcon Kind="ContentSave" Width="20" Height="20" VerticalAlignment="Center"/>
                                    <TextBlock Text="EXPORT MERGED IMAGE" Margin="10,0,0,0" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Right Panel: Preview -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{StaticResource CardBackground}" Padding="15,10" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <materialIcons:MaterialIcon Kind="InformationOutline" Width="16" Height="16" Foreground="{StaticResource AccentBrush}" Opacity="0.7"/>
                                <TextBlock Text="PREVIEW" VerticalAlignment="Center" Margin="8,0" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}"/>
                            </StackPanel>
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <materialIcons:MaterialIcon Kind="MagnifyPlusOutline" Width="18" Height="18" Margin="0,0,8,0" Opacity="0.5"/>
                                <Slider Width="150" Value="{Binding Zoom}" Minimum="0.1" Maximum="2.0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Preview Area -->
                    <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="{StaticResource SidebarBackground}">
                        <Grid Margin="40">
                            <Image Source="{Binding PreviewImage}" Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Image.LayoutTransform>
                                    <ScaleTransform ScaleX="{Binding Zoom}" ScaleY="{Binding Zoom}"/>
                                </Image.LayoutTransform>
                                <Image.Effect>
                                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.5"/>
                                </Image.Effect>
                            </Image>
                            
                            <!-- Subtle Loading Indicator (Bottom Left) -->
                            <Border HorizontalAlignment="Left" VerticalAlignment="Bottom" 
                                    Margin="20" Padding="12,8"
                                    Background="{StaticResource CardBackground}" 
                                    CornerRadius="20"
                                    Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}"
                                    Opacity="0.9">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                                </Border.Effect>
                                <StackPanel Orientation="Horizontal">
                                    <materialIcons:MaterialIcon Kind="Sync" Width="16" Height="16" 
                                                                Foreground="{StaticResource AccentBrush}"
                                                                Style="{StaticResource SpinningIconStyle}"/>
                                    <TextBlock Text="Updating preview..." Margin="8,0,0,0" 
                                               VerticalAlignment="Center" FontSize="11" 
                                               FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
