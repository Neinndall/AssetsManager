<Window x:Class="AssetsManager.Views.Dialogs.ProgressDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
        mc:Ignorable="d" FontFamily="{StaticResource AppFontFamily}"
        Background="Transparent"
        Height="300" Width="600" WindowStyle="None" AllowsTransparency="True"
        WindowStartupLocation="CenterOwner">
        
    <Border Background="{StaticResource CardBackground}"
            BorderBrush="{StaticResource BorderColor}"
            BorderThickness="1"
            CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource AccentBrush}" CornerRadius="5,5,0,0" MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <materialIcons:MaterialIcon Kind="{Binding HeaderIconKind}" Width="16" Height="16" Foreground="White" VerticalAlignment="Center" Margin="10,0,5,0"/>
                        <TextBlock Text="{Binding HeaderText}" FontWeight="Medium" FontSize="13" Foreground="White" VerticalAlignment="Center" Margin="5,10"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,10,0">
                        <Button Style="{StaticResource TitleBarButtonStyle}" Click="MinimizeButton_Click">
                            <materialIcons:MaterialIcon Kind="WindowMinimize" Width="14" Height="14"/>
                        </Button>
                        <Button Style="{StaticResource TitleBarButtonStyle}" Click="CloseButton_Click" Margin="8,0,0,0">
                            <materialIcons:MaterialIcon Kind="WindowClose" Width="14" Height="14"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Progress Information -->
            <StackPanel Grid.Row="1" Margin="30,30,12,12">
                <TextBlock x:Name="ProgressSummaryTextBlock" Text="{Binding OperationVerb}" FontWeight="Normal" FontSize="14" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                <Grid Margin="0,0,0,4">
                    <TextBlock x:Name="CurrentFileTextBlock" Text="" FontSize="12" Foreground="{StaticResource TextSecondary}" TextTrimming="CharacterEllipsis"/>
                </Grid>
                <TextBlock x:Name="EstimatedTimeTextBlock" Text="Calculating..." FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,12"/>
            </StackPanel>

            <!-- Footer with Cancel Button -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,10">
                <Button x:Name="CancelButton" Content="Cancel" Style="{StaticResource ModernButton}" Click="CancelButton_Click" Width="80"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>