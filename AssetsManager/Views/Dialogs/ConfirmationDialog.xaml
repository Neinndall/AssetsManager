<ui:FluentWindow x:Class="AssetsManager.Views.Dialogs.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:AssetsManager.Views.Dialogs"
        xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
        xmlns:ui="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
        mc:Ignorable="d" FontFamily="{StaticResource AppFontFamily}"
        Title="Confirmation" Width="400"
        SizeToContent="Height" WindowStartupLocation="CenterOwner" 
        Background="Transparent"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="None"
        ResizeMode="NoResize"
        SnapsToDevicePixels="True" UseLayoutRounding="True">

    <Border Background="{StaticResource SidebarBackground}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="Auto"/> <!-- Message -->
                <RowDefinition Height="Auto"/> <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- 1. Header (Fixed 36px) -->
            <Border Grid.Row="0" Background="{StaticResource CardBackground}" Height="36" CornerRadius="8,8,0,0" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <materialIcons:MaterialIcon Grid.Column="0" x:Name="iconType" Kind="QuestionMarkCircle" 
                                                Foreground="{StaticResource AccentBrush}" 
                                                Width="18" Height="18" Margin="15,0,0,0"
                                                VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" 
                               FontWeight="SemiBold" FontSize="12" 
                               Foreground="{StaticResource TextPrimary}" 
                               VerticalAlignment="Center" Margin="10,0,0,0"/>

                    <ui:TitleBar Grid.Column="2" Grid.ColumnSpan="2" 
                                 Title="" 
                                 Icon="{x:Null}"
                                 Height="36"
                                 Padding="0"
                                 VerticalAlignment="Stretch"
                                 Background="Transparent"
                                 ShowMaximize="False"
                                 ShowMinimize="False"/>
                </Grid>
            </Border>

            <!-- 2. Message Area (No extra gaps) -->
            <Grid Grid.Row="1" Margin="20,16,20,12"> 
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <materialIcons:MaterialIcon Kind="AlertCircleOutline" 
                                            Foreground="{StaticResource TextMuted}" 
                                            Width="32" Height="32" 
                                            VerticalAlignment="Top" 
                                            Margin="0,0,16,0"
                                            Opacity="0.5"/>

                <TextBlock Grid.Column="1" x:Name="textBlockMessage" 
                           TextWrapping="Wrap" 
                           Foreground="{StaticResource TextPrimary}" 
                           FontSize="13"
                           LineHeight="18"
                           VerticalAlignment="Center"/>
            </Grid>

            <!-- 3. Buttons Area (Tight bottom) -->
            <Grid Grid.Row="2" Margin="20,0,20,20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <StackPanel x:Name="YesNoButtons" Orientation="Horizontal">
                        <Button x:Name="btnYes" Content="Yes" Style="{StaticResource PrimaryButton}" Click="btnYes_Click" MinWidth="80" Height="32"/>
                        <Button x:Name="btnNoBottom" Content="No" Style="{StaticResource SecondaryButton}" Margin="10,0,0,0" Click="btnNo_Click" MinWidth="80" Height="32"/>
                    </StackPanel>
                    <Button x:Name="btnOk" Content="OK" Style="{StaticResource PrimaryButton}" Click="btnOk_Click" Visibility="Collapsed" MinWidth="90" Height="32"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</ui:FluentWindow>