<UserControl x:Class="AssetsManager.Views.Dialogs.Controls.NotificationHubControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:local="clr-namespace:AssetsManager.Views.Controls"
             xmlns:models="clr-namespace:AssetsManager.Views.Models.Notifications"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="350">
    
    <UserControl.Resources>
        <!-- DataTemplate para cada notificación -->
        <DataTemplate DataType="{x:Type models:NotificationModel}">
            <Border x:Name="NotificationBorder" 
                    Background="{StaticResource CardBackground}" 
                    CornerRadius="6" 
                    Margin="0,0,0,8" 
                    BorderBrush="{StaticResource BorderColor}" 
                    BorderThickness="1"
                    Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Icono según tipo -->
                    <materialIcons:MaterialIcon x:Name="TypeIcon" 
                                                Width="20" Height="20" 
                                                VerticalAlignment="Top" 
                                                Margin="0,2,12,0">
                        <materialIcons:MaterialIcon.Style>
                            <Style TargetType="materialIcons:MaterialIcon">
                                <Setter Property="Kind" Value="InformationOutline"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentBlue}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Type}" Value="Success">
                                        <Setter Property="Kind" Value="CheckCircleOutline"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Type}" Value="Warning">
                                        <Setter Property="Kind" Value="AlertOutline"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentOrange}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Type}" Value="Error">
                                        <Setter Property="Kind" Value="CloseCircleOutline"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentRed}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </materialIcons:MaterialIcon.Style>
                    </materialIcons:MaterialIcon>

                    <!-- Texto -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Title}" 
                                   FontWeight="Bold" 
                                   FontSize="13" 
                                   Foreground="{StaticResource TextPrimary}"
                                   TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding Message}" 
                                   FontSize="12" 
                                   Foreground="{StaticResource TextSecondary}" 
                                   Margin="0,2,0,0"
                                   TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm'}" 
                                   FontSize="10" 
                                   Foreground="{StaticResource TextMuted}" 
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- Botón Cerrar/Descartar -->
                    <Button Grid.Column="2" 
                            Style="{StaticResource TransparentIconButton}" 
                            Width="24" Height="24" 
                            VerticalAlignment="Top" 
                            Click="RemoveNotification_Click"
                            Margin="4,-4,-4,0">
                        <materialIcons:MaterialIcon Kind="Close" Width="14" Height="14" Foreground="{StaticResource TextMuted}"/>
                    </Button>
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding IsRead}" Value="False">
                    <Setter TargetName="NotificationBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                    <Setter TargetName="NotificationBorder" Property="BorderThickness" Value="1,1,1,1"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </UserControl.Resources>

    <Grid x:Name="MainRoot" Visibility="{Binding IsOpen, Converter={StaticResource BoolToVisibilityConverter}}">
        <!-- Overlay invisible para cerrar al clicar fuera -->
        <Border Background="Transparent" MouseLeftButtonDown="Overlay_MouseLeftButtonDown"/>
        
        <!-- El Panel Flotante -->
        <Border x:Name="MainPanel"
                Width="350" 
                MaxHeight="500" 
                Background="{StaticResource CardBackground}" 
                CornerRadius="12" 
                BorderBrush="{StaticResource BorderColor}" 
                BorderThickness="1"
                HorizontalAlignment="Right" 
                VerticalAlignment="Bottom"
                Margin="0,0,20,70">
            <Border.RenderTransform>
                <TranslateTransform x:Name="PanelTranslateTransform"/>
            </Border.RenderTransform>
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="0" Opacity="0.4" Color="Black"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Header -->
                    <RowDefinition Height="*"/>    <!-- Content -->
                    <RowDefinition Height="Auto"/> <!-- Footer -->
                </Grid.RowDefinitions>

                <!-- Header (Draggable) -->
                <Border Grid.Row="0" 
                        Padding="16,12" 
                        BorderBrush="{StaticResource BorderColor}" 
                        BorderThickness="0,0,0,1"
                        Background="Transparent"
                        Cursor="SizeAll"
                        MouseLeftButtonDown="Header_MouseLeftButtonDown"
                        MouseLeftButtonUp="Header_MouseLeftButtonUp"
                        MouseMove="Header_MouseMove">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Title -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" IsHitTestVisible="False">
                            <materialIcons:MaterialIcon Kind="BellOutline" Width="18" Height="18" Foreground="{StaticResource AccentBrush}" Margin="0,0,10,0"/>
                            <TextBlock Text="NOTIFICATIONS" FontWeight="Bold" FontSize="12" VerticalAlignment="Center" Foreground="{StaticResource TextPrimary}"/>
                            <Border Background="{StaticResource AccentRed}" 
                                    CornerRadius="8" 
                                    Padding="6,1" 
                                    Margin="8,0,0,0" 
                                    VerticalAlignment="Center"
                                    Visibility="{Binding HasUnread, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="{Binding UnreadCount}" Foreground="White" FontSize="10" FontWeight="Bold"/>
                            </Border>
                        </StackPanel>

                        <!-- Window Controls -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Style="{StaticResource TransparentIconButton}" ToolTip="Minimize" Click="Minimize_Click" Width="24" Height="24" Margin="0,0,4,0">
                                <materialIcons:MaterialIcon Kind="WindowMinimize" Width="14" Height="14" Foreground="{StaticResource TextSecondary}"/>
                            </Button>
                            <Button Style="{StaticResource TransparentIconButton}" ToolTip="Close" Click="Close_Click" Width="24" Height="24">
                                <materialIcons:MaterialIcon Kind="WindowClose" Width="14" Height="14" Foreground="{StaticResource TextSecondary}"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Content Area -->
                <ScrollViewer Grid.Row="1" Margin="12,12,8,12" VerticalScrollBarVisibility="Auto">
                    <Grid>
                         <!-- Actions Toolbar (Inside Content) -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,8,8">
                            <Button Style="{StaticResource TransparentIconButton}" ToolTip="Mark all as read" Click="MarkAllRead_Click" Margin="0,0,4,0">
                                <StackPanel Orientation="Horizontal">
                                    <materialIcons:MaterialIcon Kind="CheckAll" Width="16" Height="16" Foreground="{StaticResource TextSecondary}" Margin="0,0,4,0"/>
                                    <TextBlock Text="Read All" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource TransparentIconButton}" ToolTip="Clear all" Click="ClearAll_Click">
                                <StackPanel Orientation="Horizontal">
                                    <materialIcons:MaterialIcon Kind="DeleteSweepOutline" Width="16" Height="16" Foreground="{StaticResource TextSecondary}" Margin="0,0,4,0"/>
                                    <TextBlock Text="Clear All" FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        
                        <!-- Empty State -->
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,40"
                                    Visibility="{Binding Notifications.Count, Converter={StaticResource InverseBooleanToVisibilityConverter}, ConverterParameter=0}">
                            <materialIcons:MaterialIcon Kind="BellOffOutline" Width="48" Height="48" Foreground="{StaticResource TextMuted}" Opacity="0.3"/>
                            <TextBlock Text="No notifications yet" Foreground="{StaticResource TextMuted}" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <!-- Notifications List -->
                        <ItemsControl ItemsSource="{Binding Notifications}" Margin="0,32,0,0"/>
                    </Grid>
                </ScrollViewer>

                <!-- Footer / Action bar -->
                <Border Grid.Row="2" Padding="12,8" Background="{StaticResource HoverColor}" CornerRadius="0,0,12,12">
                    <TextBlock Text="Showing recent updates and alerts" FontSize="10" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
